Subscripting: the value under key $v'$ of a dictionary, or the element at index $v'$ of a sequence,
counting from the end when the index is negative. It aborts with \pyinline{KeyError},
\pyinline{IndexError} or \pyinline{TypeError} where Python does.
