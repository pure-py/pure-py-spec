% Annex of auxiliary functions (specification only). Each entry states the function's meaning and the
% cases its rows do not show; the rows are shared with the paper's figures.
\chapter{Auxiliary functions}
\label{ch:auxiliary-functions}

\section{Syntactic analyses}
\label{sec:mf-syntactic}

The analyses $\assignsS$, $\binds$, $\capturesS$ and $\capturesE$ introduced in \secref{wf-aux} are
defined by their principal cases; the remaining cases are stated with each.

\begin{auxfunction}{\assignsS(s)}{set of variables}{aux:assigns}
The variables assigned anywhere in $s$, not descending into nested function definitions, which have their
own scope. It is empty on the statements not shown and is the union over the branch blocks of a
conditional. It extends by union from statements to blocks, with the singleton block identified with its
statement, and on a module body $\seq{\iota}\;\seq{s}$ acts on $\seq{s}$.
\begin{align*}
\input{functions/assigns}
\end{align*}
\end{auxfunction}

\begin{auxfunction}{\binds(p)}{set of variables}{aux:binds}
The variables introduced by pattern $p$. On the cases not shown it recurses structurally, taking the union
over the immediate subpatterns.
\begin{align*}
\input{functions/binds}
\end{align*}
\end{auxfunction}

\begin{auxfunction}{\capturesS(s)}{set of variables}{aux:captures}
The variables of the enclosing scope captured by closures (lambdas or nested function definitions) within
$s$. On the cases not shown it takes the union of $\capturesE$ over the statement's subexpressions and of
$\capturesS$ over the blocks it contains, and it extends to blocks and module bodies as $\assignsS$ does.
\begin{align*}
\input{functions/captures}
\end{align*}
\end{auxfunction}

\begin{auxfunction}{\capturesE(e)}{set of variables}{aux:captures-e}
The variables of the enclosing scope captured by closures within expression $e$. On the cases not shown it
recurses structurally, taking the union over the immediate subexpressions.
\begin{align*}
\input{functions/captures-e}
\end{align*}
\end{auxfunction}

\begin{auxfunction}{\fvS(b), \fvE(e)}{set of variables}{aux:fv}
The free variables of a block and of an expression, defined in the usual way.
\end{auxfunction}

\section{Classes and modules}
\label{sec:mf-classes}

The functions on module names, field lists and class entries introduced in \secref{modules-programs}, and
the resolution of qualified names in an environment.

\begin{auxfunction}{\submods(q)}{context}{aux:submods}
The context that binds each submodule of $q$ to a stub: an entry $\bind{x}{\modStub{q.x}}$ for each module
$q.x$ of the program or of the predefined modules.
\begin{align*}
\input{functions/submods}
\end{align*}
\end{auxfunction}

\begin{auxfunction}{\ownFields(\phi)}{sequence of variables}{aux:own-fields}
The field names a field list declares, in order.
\begin{align*}
\input{functions/own-fields}
\end{align*}
\end{auxfunction}

\begin{auxfunction}{\fields(C)}{sequence of variables}{aux:fields}
All fields of class $C$: the fields of its base class, looked up in the declaring context, followed by
its own.
\begin{align*}
\input{functions/fields}
\end{align*}
\end{auxfunction}

\begin{auxfunction}{\ancestors(C)}{set of class entries}{aux:ancestors}
Class $C$ together with all its base classes.
\begin{align*}
\input{functions/ancestors}
\end{align*}
\end{auxfunction}

\begin{auxfunction}{\fieldMap(C, \seq{u})}{map from variables}{aux:field-map}
The argument supplied for each field of $C$: positional arguments fill the fields in order and keyword
arguments name theirs. It is undefined unless the arguments cover the fields exactly, so constructor
patterns and expressions check and evaluate field by field without regard to how a constructor splits its
arguments.
\begin{align*}
\input{functions/field-map}
\end{align*}
\end{auxfunction}

\begin{auxfunction}{\env(\rho, q)}{environment}{aux:env}
The environment of module $q$ as loaded in $\rho$, reached by following the module references along $q$.
\begin{align*}
\input{functions/env}
\end{align*}
\end{auxfunction}

\begin{auxfunction}{\resolveClass(\rho, q)}{class entry}{aux:resolve-class}
The class entry that qualified name $q$ denotes in $\rho$: a bare class name is looked up directly, a
qualified one in the environment of its module.
\begin{align*}
\input{functions/resolve-class}
\end{align*}
\end{auxfunction}

\begin{auxfunction}{\nameOf(q)}{string}{aux:name}
The string literal naming module $q$, so that $\nameOf(\pymath{\_\_main\_\_}) = \pymath{"\_\_main\_\_"}$.
\end{auxfunction}

\section{Values}
\label{sec:mf-values}

\input{tex/auxiliary-values}
