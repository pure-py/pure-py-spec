[project]
name = "pure-py-reference-checker"
version = "0.15.1"
requires-python = ">=3.12"

[dependency-groups]
dev = [
    "mypy>=2.3.1",
    "ruff>=0.16.2",
]

[tool.mypy]
python_version = "3.12"
strict = true

[tool.ruff]
target-version = "py312"
