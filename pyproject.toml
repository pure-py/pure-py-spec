[project]
name = "pure-py-reference-checker"
version = "0.17.0"
requires-python = ">=3.12"

[dependency-groups]
dev = [
    "mypy>=2.3.1",
    "ruff>=0.16.2",
]

[tool.mypy]
strict = true

[tool.ruff]
line-length = 100
