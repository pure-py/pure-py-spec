\subsection{Partiality}
\label{sec:partiality}

\input{spec/functions/operators-text}

Structural equality $\eqOp$ compares literals by value, lists and tuples elementwise, dictionaries with
the same keys by their values, and objects of the same class by their fields; \pyinline{None} compares
with any value. Otherwise equality is undefined: between values of different kinds, such as a number and a
string, a list and a tuple, or a Boolean and a number; between functions; and between two
\pyinline{nan}s inside a container. Directly, \pyinline{nan == nan} is \pyinline{False} in both languages,
but Python compares container elements by identity before equality, so two lists holding \pyinline{nan}s
are equal or not according to whether they hold the same object, a distinction \PurePy cannot make.
Python gives an answer in each case (\figref{coercions}), so each is a dynamically excluded comparison. Comparison short-circuits at the first element that
decides the result, so the comparison order is observable:

\begin{python}
print([1, "a"] == [2, 3])  # False, decided at the first element
print([1, "a"] == [1, 3])  # undefined: "a" against 3
\end{python}

\noindent Dictionaries are compared in the entry order of the left operand for the same reason. The membership
function $\memOp$ tests an element of a list or tuple by structural equality, short-circuiting in the same
way, a key of a dictionary, or a substring of a string. The strict operators are given by
$\hat{\odot}$ and $\hat{\ominus}$ (\figref{operators}); the definitions of all of these are given in
\specification.

\input{paper/fig/operators}

The remaining metafunctions the rules use are collected in \figref{eval-aux}: $\outcome$ reads a
statement's result as the outcome of calling it, $\iterOp$ gives the sequence a generator draws from, $\getitem$
interprets subscripting, $\dispatch$ selects the case a $\pyinline{match}$ takes, and $\entries$ and
$\update$ build a dictionary from its entries.

\input{paper/fig/eval-aux}

A qualified name is resolved through the environments of loaded modules: $\env(\rho, q)$ is the
environment of module $q$ as loaded in $\rho$, and $\resolveClass(\rho, q)$ the class entry that $q$
denotes (\figref{resolution}).

\input{paper/fig/resolution}
