For example, a list pattern matches a list of the same length elementwise, composing the sub-results,
and fails to match a list of another length or a value that is not a sequence. A tuple is neither, so a
list pattern and a tuple are not comparable: there is no derivation, and the run has no result:
\begin{mathpar}
\input{spec/rules/eval-pat-list}
\and
\input{spec/rules/eval-pat-list-no}
\end{mathpar}
