\begin{figure}
\begin{subfigure}{\textwidth}
\centering
\small
\begin{align*}
\imports(\exImport{q}) &= \set{q} \\
\imports(\exFromImport{q}{x_1, \ldots, x_{n+1}}) &= \set{q} \cup \set{q.x_i \mid x_i \in \dom(\submodules(q))} \\
\\
\\
\deps_{q}(\exImport{q'}) &= \imports(\exImport{q'}) \cup \set{p \mid p \properPrefixOf q', p \neq q} \\
\deps_{q}(\exFromImport{q'}{\seq{x}}) &= \imports(\exFromImport{q'}{\seq{x}}) \cup \set{p \mid p \properPrefixOf q', p \not\prefixOf q} \\
\\
\submodules(q) &= \set{\bind{x}{\modStub{q.x}} \mid q.x \in \dom(\mathcal{M})}
\end{align*}
\subcaption{$\imports$,  $\deps$, and $\submodules$ metafunctions}
\end{subfigure}

\vspace{2mm}

\begin{subfigure}{\textwidth}
\centering
\small
\begin{align*}
\ownFields(\exField{x_1} \;\ldots\; \exField{x_{n+1}}) &= x_1, \ldots, x_{n+1} \\
\ownFields(\pyinline{pass}) &= \varepsilon \\
\\
\fields(\clsEntry{\Gamma}{q\added{.c}}{\seq{x}}{\bot}) &= \seq{x} \\
\fields(\clsEntry{\Gamma}{q\added{.c}}{\seq{x}}{c'}) &= \fields(\Gamma(c')) \mathbin{\cdot} \seq{x} \\
\\
\added{\ancestors(\clsEntry{\Gamma}{q.c}{\seq{x}}{\bot})} &\;\added{= \set{\clsEntry{\Gamma}{q.c}{\seq{x}}{\bot}}} \\
\added{\ancestors(\clsEntry{\Gamma}{q.c}{\seq{x}}{c'})} &\;\added{= \set{\clsEntry{\Gamma}{q.c}{\seq{x}}{c'}} \cup \ancestors(\Gamma(c'))}
\end{align*}
\subcaption{$\ownFields$, $\fields$\added{, and $\ancestors$} metafunctions}
\end{subfigure}

\caption{Module-related metafunctions}
\label{fig:modules}
\end{figure}
