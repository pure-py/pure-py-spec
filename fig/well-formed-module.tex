\begin{figure}
\flushleft \shadebox{$\Gamma, \vec{x} \importsR \Gamma'$}
\begin{mathpar}
\small
\inferrule*[lab={\ruleName{imp-nil}}]
{
  \strut
}
{
  \Gamma, \varepsilon \importsR \varnothing
}
\and
\inferrule*[lab={\ruleName{imp-member}}]
{
  \Gamma \vdash x \names \theta \\
  \Gamma, \vec{x} \importsR \Gamma'
}
{
  \Gamma, x \cons \vec{x} \importsR \set{\bind{x}{\theta}} \override \Gamma'
}
\and
\inferrule*[lab={\ruleName{imp-submodule}}]
{
  \bind{x}{\stub{q}} \in \Gamma \\
  \vdash_{\mathsf{load}} q : \Delta \\
  \Gamma, \vec{x} \importsR \Gamma'
}
{
  \Gamma, x \cons \vec{x} \importsR \set{\bind{x}{\loaded{q}{\Delta}}} \override \Gamma'
}
\end{mathpar}

\caption{Names $\vec{x}$ imported from module context $\Gamma$ produce bindings $\Gamma'$}
\label{fig:imports}
\end{figure}

\begin{figure}
\begin{subfigure}{\textwidth}
\flushleft \shadebox{$\vdash \vec{\iota} : \Gamma$}
\begin{mathpar}
\small
\inferrule*[lab={\ruleName{imports-nil}}]
{
  \strut
}
{
  \vdash \varepsilon : \varnothing
}
\and

\inferrule*[lab={\ruleName{import}}]
{
  q = x \cons \seq{x} \\
  \vdash_{\mathsf{load}} q : \Delta \\
  q, \loaded{q}{\Delta} \loadsTo \theta \\
  \vdash \vec{\iota} : \Gamma
}
{
  \vdash \exImport{q} \cons \vec{\iota} : \set{\bind{x}{\theta}} \extend \Gamma
}
\and
\inferrule*[lab={\ruleName{from-import}}]
{
  \vdash_{\mathsf{load}} q : \Delta \\
  q, \loaded{q}{\Delta} \loadsTo \theta \\
  \Delta, \vec{x} \importsR \Gamma' \\
  \vdash \vec{\iota} : \Gamma
}
{
  \vdash \exFromImport{q}{\vec{x}} \cons \vec{\iota} : \Gamma' \extend \Gamma
}

\end{mathpar}

\flushleft \shadebox{$q, \theta \loadsTo \theta'$}
\begin{mathpar}
\small
\inferrule*[lab={\ruleName{loads-to-simple}}]
{
  \strut
}
{
  x, \theta \loadsTo \theta
}
\and
\inferrule*[lab={\ruleName{loads-to-qualified}}]
{
  \vdash_{\mathsf{load}} q : \Gamma' \\
  q, \loaded{q}{\Gamma' \extend \set{\bind{x}{\theta}}} \loadsTo \theta'
}
{
  q.x, \theta \loadsTo \theta'
}
\end{mathpar}
\subcaption{Import prefix $\vec{\iota}$ produces bindings $\Gamma$; loaded reference $\theta$ for module $q$ loads to the reference for the head of $q$}
\end{subfigure}

\vspace{2mm}

\begin{subfigure}{\textwidth}
\flushleft \shadebox{$\Gamma \vdash \vec{s} : \Delta$}
\begin{mathpar}
\small
\inferrule*[lab={\ruleName{nil}}]
{
  \strut
}
{
  \Gamma \vdash \varepsilon : \varnothing
}
\and
\inferrule*[lab={\ruleName{statement}}]
{
  \Gamma \vdash s : \Assigns{\Delta} \\
  \Gamma \override \Delta \vdash \vec{s} : \Delta' \\
  \capturesS(s) \cap \assignsS(\vec{s}) = \varnothing
}
{
  \Gamma \vdash s \cons \vec{s} : \Delta \override \Delta'
}
\and
\inferrule*[lab={\ruleName{class}}]
{
  \ownFields(\phi) = \vec{x} \text{ distinct} \\
  C = \clsEntry{\Gamma}{q}{\vec{x}}{\bot} \\
  \Gamma, \bind{c}{C} \vdash \vec{s} : \Delta
}
{
  \Gamma \vdash \exDataclass{c}{\phi} \cons \vec{s} : \set{\bind{c}{C}} \override \Delta
}
\and
\inferrule*[lab={\ruleName{class-extend}}]
{
  \bind{c'}{\clsEntry{\Delta}{q}{\vec{y}}{c''}} \in \Gamma \\
  \ownFields(\phi) = \vec{x} \text{ distinct} \\
  \vec{x} \cap \fields(\Gamma(c')) = \varnothing \\
  C = \clsEntry{\Gamma}{q}{\vec{x}}{c'} \\
  \Gamma, \bind{c}{C} \vdash \vec{s} : \Delta
}
{
  \Gamma \vdash \exDataclassExtend{c}{c'}{\phi} \cons \vec{s} : \set{\bind{c}{C}} \override \Delta
}
\end{mathpar}
\subcaption{Statements $\vec{s}$ of module $q$ are well-formed in $\Gamma$}
\end{subfigure}

\vspace{2mm}

\begin{subfigure}{\textwidth}
\flushleft \shadebox{$\vdash_{\mathsf{load}} q : \Gamma$}
\begin{mathpar}
\small

\inferrule*[lab={\ruleName{module}}]
{
  \mathcal{M}(q) = \vec{\iota}\;\vec{s} \\
  \vdash \vec{\iota} : \Gamma \\
  \Gamma \vdash (\exAssign{\pymath{\_\_name\_\_}}{\nameOf(q)}) \cons \vec{s} : \Delta \\
  (\dom(\Gamma) \cup \dom(\Delta)) \cap \dom(\submodules(q)) = \varnothing
}
{
  \vdash_{\mathsf{load}} q : \submodules(q) \override \Delta
}

\end{mathpar}

\flushleft \shadebox{$\vdash \mathcal{M}$}
\begin{mathpar}
\small
\inferrule*[lab={\ruleName{program}}]
{
  \vdash_{\mathsf{load}} \pymath{\_\_main\_\_} : \Delta \\
  G_{\mathcal{M}} \text{ acyclic}
}
{
  \vdash \mathcal{M}
}
\end{mathpar}
\subcaption{Module and program well-formedness}
\end{subfigure}
\caption{Module well-formedness}
\label{fig:well-formed-module}
\end{figure}
