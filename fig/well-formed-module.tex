\begin{figure}
\flushleft \shadebox{$\Gamma, x \importsR \theta$} \quad {\color{deletedgray}\shadebox{$\Gamma, \seq{x} \importsR \Gamma'$}}
\begin{mathpar}
\small
{\color{deletedgray}
\inferrule*[lab={\ruleName{imp-nil}}]
{
  \strut
}
{
  \Gamma, \varepsilon \importsR \varnothing
}}
\and
{\color{deletedgray}
\inferrule*[lab={\ruleName{imp-member}}]
{
  \Gamma \vdash x \names \theta \\
  \Gamma, \seq{x} \importsR \Gamma'
}
{
  \Gamma, x \cons \seq{x} \importsR \set{\bind{x}{\theta}} \override \Gamma'
}}
\and
{\color{deletedgray}
\inferrule*[lab={\ruleName{imp-submodule}}]
{
  \bind{x}{\modStub{q}} \in \Gamma \\
  \vdash_{\mathsf{load}} q : \Delta \\
  \Gamma, \seq{x} \importsR \Gamma'
}
{
  \Gamma, x \cons \seq{x} \importsR \set{\bind{x}{\modLoaded{q}{\Delta}}} \override \Gamma'
}}
\and
{\color{blue}
\inferrule*[lab={\ruleName{imp-member}}]
{
  \Gamma \vdash x \names \theta
}
{
  \Gamma, x \importsR \theta
}}
\and
{\color{blue}
\inferrule*[lab={\ruleName{imp-submodule}}]
{
  \bind{x}{\modStub{q}} \in \Gamma \\
  \vdash_{\mathsf{load}} q : \Delta
}
{
  \Gamma, x \importsR \modLoaded{q}{\Delta}
}}
\end{mathpar}

\caption{\added{Name $x$ imported from module context $\Gamma$ has entry $\theta$}\deleted{Names $\seq{x}$ imported from module context $\Gamma$ produce bindings $\Gamma'$}}
\label{fig:imports}
\end{figure}

\begin{figure}
\begin{subfigure}{\textwidth}
\flushleft \shadebox{$\vdash \seq{\iota} : \Gamma$}
\begin{mathpar}
\small
\inferrule*[lab={\ruleName{imports-nil}}]
{
  \strut
}
{
  \vdash \varepsilon : \varnothing
}
\and

\inferrule*[lab={\ruleName{import}}]
{
  q' = x \cons \seq{x} \\
  q \not\properPrefixOf q' \\
  \vdash_{\mathsf{load}} q' : \Delta \\
  \added{q', \modLoaded{q'}{\Delta} \linksTo \theta}\deleted{\varepsilon, q', \modLoaded{q'}{\Delta} \loadsTo \theta} \\
  \vdash \seq{\iota} : \Gamma
}
{
  \vdash \exImport{q'} \cons \seq{\iota} : \set{\bind{x}{\theta}} \extend \Gamma
}
\and
\inferrule*[lab={\ruleName{from-import}}]
{
  \vdash_{\mathsf{load}} q' : \Delta \\
  \added{\vdash_{\mathsf{load}} p : \Delta_p \quad (\forall p \properPrefixOf q',\; p \not\prefixOf q)}\deleted{q, q', \modLoaded{q'}{\Delta} \loadsTo \theta} \\
  \added{\Delta, x_i \importsR \theta_i \quad (\forall i)}\deleted{\Delta, \seq{x} \importsR \Gamma'} \\
  \vdash \seq{\iota} : \Gamma
}
{
  \vdash \exFromImport{q'}{x_1, \ldots, x_{n+1}} \cons \seq{\iota} : \added{\set{\bind{x_1}{\theta_1}, \ldots, \bind{x_{n+1}}{\theta_{n+1}}}}\deleted{\Gamma'} \extend \Gamma
}

\end{mathpar}

\subcaption{Import prefix $\seq{\iota}$ of module $q$ produces bindings $\Gamma$}
\end{subfigure}

\vspace{2mm}

\begin{subfigure}{\textwidth}
\flushleft \shadebox{$q, \theta \linksTo \theta'$} \quad {\color{deletedgray}\shadebox{$\seq{x}, q, \theta \loadsTo \theta'$}}
\begin{mathpar}
\small
{\color{deletedgray}
\inferrule*[lab={\ruleName{loads-to-simple}}]
{
  \strut
}
{
  \seq{x}, x, \theta \loadsTo \theta
}}
\and
{\color{deletedgray}
\inferrule*[lab={\ruleName{loads-to-qualified}}]
{
  q \not\prefixOf \seq{x} \\
  \vdash_{\mathsf{load}} q : \Gamma' \\
  \seq{x}, q, \modLoaded{q}{\Gamma' \extend \set{\bind{x}{\theta}}} \loadsTo \theta'
}
{
  \seq{x}, q.x, \theta \loadsTo \theta'
}}
\and
{\color{deletedgray}
\inferrule*[lab={\ruleName{loads-to-enclosing}}]
{
  q \prefixOf \seq{x}
}
{
  \seq{x}, q.x, \theta \loadsTo \theta
}}
\and
{\color{blue}
\inferrule*[lab={\ruleName{links-simple}}]
{
  \strut
}
{
  x, \theta \linksTo \theta
}}
\and
{\color{blue}
\inferrule*[lab={\ruleName{links-qualified}}]
{
  \vdash_{\mathsf{load}} q : \Gamma' \\
  q, \modLoaded{q}{\Gamma' \extend \set{\bind{x}{\theta}}} \linksTo \theta'
}
{
  q.x, \theta \linksTo \theta'
}}
\end{mathpar}
\subcaption{\added{Reference $\theta$ for module $q$ links to the reference $\theta'$ for the root of $q$, loading the proper prefixes of $q$}\deleted{Loaded reference $\theta$ for module $q$ loads to the reference for the head of $q$}}
\end{subfigure}
\caption{Import well-formedness}
\label{fig:well-formed-imports}
\end{figure}

\begin{figure}
\begin{subfigure}{\textwidth}
\flushleft \shadebox{$\Gamma \vdash \seq{s} : \Delta$}
\begin{mathpar}
\small
\inferrule*[lab={\ruleName{nil}}]
{
  \strut
}
{
  \Gamma \vdash \varepsilon : \varnothing
}
\and
\inferrule*[lab={\ruleName{statement}}]
{
  \Gamma \vdash s : \Assigns{\Delta} \\
  \Gamma \override \Delta \vdash \seq{s} : \Delta' \\
  \capturesS(s) \cap \assignsS(\seq{s}) = \varnothing
}
{
  \Gamma \vdash s \cons \seq{s} : \Delta \override \Delta'
}
\and
\inferrule*[lab={\ruleName{class}}]
{
  \nexists C'.\ \bind{c}{C'} \in \Gamma \\
  \ownFields(\phi) = \seq{x} \text{ distinct} \\
  C = \clsEntry{\Gamma}{q.c}{\seq{x}}{\bot} \\
  \Gamma, \bind{c}{C} \vdash \seq{s} : \Delta
}
{
  \Gamma \vdash \exDataclass{c}{\phi} \cons \seq{s} : \set{\bind{c}{C}} \override \Delta
}
\and
\inferrule*[lab={\ruleName{class-extend}}]
{
  \nexists C'.\ \bind{c}{C'} \in \Gamma \\
  \bind{c'}{\clsEntry{\Delta}{q.c'}{\seq{y}}{c''}} \in \Gamma \\
  \ownFields(\phi) = \seq{x} \text{ distinct} \\
  \seq{x} \cap \fields(\Gamma(c')) = \varnothing \\
  C = \clsEntry{\Gamma}{q.c}{\seq{x}}{c'} \\
  \Gamma, \bind{c}{C} \vdash \seq{s} : \Delta
}
{
  \Gamma \vdash \exDataclassExtend{c}{c'}{\phi} \cons \seq{s} : \set{\bind{c}{C}} \override \Delta
}
\end{mathpar}
\subcaption{Statements $\seq{s}$ of module $q$ are well-formed in $\Gamma$}
\end{subfigure}

\vspace{2mm}

\begin{subfigure}{\textwidth}
\flushleft \shadebox{$\vdash_{\mathsf{load}} q : \Gamma$}
\begin{mathpar}
\small

{\color{blue}
\inferrule*[lab={\ruleName{predefined}}]
{
  q \text{ predefined}
}
{
  \vdash_{\mathsf{load}} q : \Gamma_q
}}
\and
\inferrule*[lab={\ruleName{module}}]
{
  \mathcal{M}(q) = \seq{\iota}\;\seq{s} \\
  \vdash \seq{\iota} : \Gamma \\
  \Gamma \vdash (\exAssign{\pymath{\_\_name\_\_}}{\nameOf(q)}) \cons \seq{s} : \Delta \\
  (\dom(\Gamma) \cup \dom(\Delta)) \cap \dom(\submodules(q)) = \varnothing
}
{
  \vdash_{\mathsf{load}} q : \submodules(q) \override \Delta
}

\end{mathpar}

\flushleft \shadebox{$\vdash \mathcal{M}$}
\begin{mathpar}
\small
\inferrule*[lab={\ruleName{program}}]
{
  \vdash_{\mathsf{load}} \pymath{\_\_main\_\_} : \added{\Gamma}\deleted{\Delta} \\
  {\color{deletedgray}G_{\mathcal{M}} \text{ acyclic}}
}
{
  \vdash \mathcal{M}
}
\end{mathpar}
\subcaption{Module and program well-formedness}
\end{subfigure}
\caption{Module well-formedness}
\label{fig:well-formed-module}
\end{figure}
