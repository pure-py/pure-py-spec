\begin{figure}
\flushleft \shadebox{$\Gamma \vdash p_1 \cons \ldots \cons p_{n+1}$}
\begin{mathpar}
\small
\inferrule*[lab={\ruleName{pat-lit}}]
{ \strut }
{ \Gamma \vdash \ell }
\and
\inferrule*[lab={\ruleName{pat-neg-lit}}]
{ \strut }
{ \Gamma \vdash -n }
\and
\inferrule*[lab={\ruleName{pat-var}}]
{ \strut }
{ \Gamma \vdash x }
\and
\inferrule*[lab={\ruleName{pat-wild}}]
{ \strut }
{ \Gamma \vdash \patWild }
\and
\inferrule*[lab={\ruleName{pat-list}}]
{ \Gamma \vdash \replaced{p_i \quad (\forall i)}{p \quad (\forall p \in \seq{p})\ } \\ \added{\text{variables in } \seq{p} \text{ distinct}} }
{ \Gamma \vdash \patList{\seq{p}} }
\and
\inferrule*[lab={\ruleName{pat-tuple}}]
{ \Gamma \vdash \replaced{p_i \quad (\forall i)}{p \quad (\forall p \in \seq{p})\ } \\ \added{\text{variables in } \seq{p} \text{ distinct}} }
{ \Gamma \vdash \patTuple{\seq{p}} }
\and
\inferrule*[lab={\ruleName{pat-as}}]
{ \Gamma \vdash p \\ \added{x \notin \binds(p)} }
{ \Gamma \vdash p\;\pyinline{as}\;x }
\and
\inferrule*[lab={\ruleName{pat-dict}}]
{ \Gamma \vdash p_i \quad (\forall i) \\ w_1, \ldots, w_n \text{ distinct} \\ \added{\text{variables in } \seq{p} \text{ distinct}} }
{ \Gamma \vdash \patDict{\bind{w_1}{p_1}, \ldots, \bind{w_n}{p_n}} }
\and
\inferrule*[lab={\ruleName{pat-constr}}]
{
  \Gamma \vdash q : C \\
  \fields(C) = x_1, \ldots, x_n \cons \seq{x}\,' \\
  \set{\seq{y}} = \set{\seq{x}\,'} \\
  \Gamma \vdash p_i \quad (\forall i) \\
  \added{\text{variables in } \seq{p} \text{ distinct}}
}
{
  \Gamma \vdash \patConstr{q}{p_1, \ldots, p_n, y_1 \mathord{=} p_{n+1}, \ldots, y_m \mathord{=} p_{n+m}}
}
\and
\inferrule*[lab={\ruleName{cases-cons}}]
{
  \Gamma \vdash p \\
  \Gamma \vdash \replaced{p' \cons \seq{p}\,'}{\seq{p}\,'\ } \\
  \replaced{p_i \not\subsumed p \quad (\forall i)}{p' \not\subsumed p \quad (\forall p' \in \seq{p}\,')\ }
}
{
  \Gamma \vdash p \cons \added{p'} \mathbin{\added{\cons}} \seq{p}\,'
}
\end{mathpar}
\caption{Case patterns are well-formed under $\Gamma$}
\label{fig:well-formed-pat}
\end{figure}
