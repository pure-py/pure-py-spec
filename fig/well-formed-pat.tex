\begin{figure}
\flushleft \shadebox{$\Gamma \vdash p_1 \cons \ldots \cons p_{n+1}$}
\begin{mathpar}
\small
\inferrule*[lab={\ruleName{pat-lit}}]
{ \strut }
{ \Gamma \vdash \ell }
\and
\inferrule*[lab={\ruleName{pat-neg-lit}}]
{ \strut }
{ \Gamma \vdash -n }
\and
\inferrule*[lab={\ruleName{pat-var}}]
{ \strut }
{ \Gamma \vdash x }
\and
\inferrule*[lab={\ruleName{pat-wild}}]
{ \strut }
{ \Gamma \vdash \patWild }
\and
\inferrule*[lab={\ruleName{pat-list}}]
{ \Gamma \vdash p_i \quad (\forall i) \\ \text{variables in } \seq{p} \text{ distinct} }
{ \Gamma \vdash \patList{\seq{p}} }
\and
\inferrule*[lab={\ruleName{pat-tuple}}]
{ \Gamma \vdash p_i \quad (\forall i) \\ \text{variables in } \seq{p} \text{ distinct} }
{ \Gamma \vdash \patTuple{\seq{p}} }
\and
\inferrule*[lab={\ruleName{pat-as}}]
{ \Gamma \vdash p \\ x \notin \binds(p) }
{ \Gamma \vdash p\;\pyinline{as}\;x }
\and
\inferrule*[lab={\ruleName{pat-dict}}]
{ \Gamma \vdash p_i \quad (\forall i) \\ w_1, \ldots, w_n \text{ distinct} \\ \text{variables in } \seq{p} \text{ distinct} }
{ \Gamma \vdash \patDict{\bind{w_1}{p_1}, \ldots, \bind{w_n}{p_n}} }
\and
\inferrule*[lab={\ruleName{pat-constr}}]
{
  \Gamma \vdash q : C \\
   \\
   \\
  \Gamma \vdash \fieldMap(C, \seq{p})(x) \quad (\forall x \in \fields(C)) \\
  \text{variables in } \seq{p} \text{ distinct}
}
{
  \Gamma \vdash \patConstr{q}{\seq{p}}
}
\and
\inferrule*[lab={\ruleName{cases-cons}}]
{
  \Gamma \vdash p \\
  \Gamma \vdash p' \cons \seq{p}\,' \\
  \Gamma \vdash p_i \not\subsumed p \quad (\forall i)
}
{
  \Gamma \vdash p \cons p' \mathbin{\cons} \seq{p}\,'
}
\end{mathpar}
\caption{Case patterns are well-formed under $\Gamma$}
\label{fig:well-formed-pat}
\end{figure}
