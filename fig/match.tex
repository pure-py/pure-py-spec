\begin{figure}
\flushleft \shadebox{$\rho, p, v \matches \rho'$}
\begin{mathpar}
\small
\inferrule*[lab={\ruleName{eval-pat-lit}}]
{
  \strut
}
{
  \rho, \ell, \ell \matches \varnothing
}
\and
\inferrule*[lab={\ruleName{eval-pat-neg-lit}}]
{
  \strut
}
{
  \rho, -n, -n \matches \varnothing
}
\and
\inferrule*[lab={\ruleName{eval-pat-var}}]
{
  \strut
}
{
  \rho, x, v \matches \set{\bind{x}{v}}
}
\and
\inferrule*[lab={\ruleName{eval-pat-wild}}]
{
  \strut
}
{
  \rho, \patWild, v \matches \varnothing
}
\and
\inferrule*[lab={\ruleName{eval-pat-list}}]
{
  \rho, p_i, v_i \matches \rho_i \quad (\forall i)
}
{
  \rho, \patList{p_1, \ldots, p_n}, \exList{v_1, \ldots, v_n} \matches \rho_1 \disjunion \ldots \disjunion \rho_n
}
\and
\inferrule*[lab={\ruleName{eval-pat-tuple}}]
{
  \rho, p_i, v_i \matches \rho_i \quad (\forall i)
}
{
  \rho, \patTuple{p_1, \ldots, p_n}, \exTuple{v_1, \ldots, v_n} \matches \rho_1 \disjunion \ldots \disjunion \rho_n
}
\and
\inferrule*[lab={\ruleName{eval-pat-dict}}]
{
  \bind{w_i}{\replaced{v_i}{u_i}} \in \delta \quad (\forall i) \\
  \rho, p_i, \replaced{v_i}{u_i} \matches \rho_i \quad (\forall i)
}
{
  \rho, \patDict{\bind{w_1}{p_1}, \ldots, \bind{w_n}{p_n}}, \delta \matches \rho_1 \disjunion \ldots \disjunion \rho_n
}
\and
\inferrule*[lab={\ruleName{eval-pat-as}}]
{
  \rho, p, v \matches \rho'
}
{
  \rho, p\;\pyinline{as}\;x, v \matches \rho' \disjunion \set{\bind{x}{v}}
}
\and
\inferrule*[lab={\ruleName{eval-pat-constr}}]
{
  \resolveClass(\rho, q) = C \\
  C \in \ancestors(C') \\
  \replaced{\rho, \fieldMap(C, \seq{p})(x), \rho^\dagger(x) \matches \rho_x \quad (\forall x \in \fields(C))}{\fields(C) = x_1, \ldots, x_n, x_{n+1}, \ldots, x_{n+m}\ } \\
  \deleted{\rho, p_i, \rho^\dagger(x_i) \matches \rho_i \quad (\forall i.\, 1 \le i \le n)} \\
  \deleted{\rho, p_{n+j}, \rho^\dagger(y_j) \matches \rho_{n+j} \quad (\forall j.\, 1 \le j \le m)}
}
{
  \rho, \patConstr{q}{\replaced{\seq{p}}{p_1, \ldots, p_n, y_1 \mathord{=} p_{n+1}, \ldots, y_m \mathord{=} p_{n+m}\ }}, \exObj{\Gamma'}{C'}{\rho^\dagger} \matches \replaced{{\textstyle\biguplus_{x \in \fields(C)}}\, \rho_x}{\rho_1 \disjunion \ldots \disjunion \rho_{n+m}\ }
}
\end{mathpar}
\caption{Pattern $p$ matches value $v$ under environment $\rho$, producing bindings $\rho'$}
\label{fig:match}
\end{figure}
