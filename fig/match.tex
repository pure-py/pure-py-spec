\begin{figure}
\flushleft \shadebox{$\rho, p, v \matches \rho'$}
\begin{mathpar}
\small
\inferrule*[lab={\ruleName{pat-lit}}]
{
  \strut
}
{
  \rho, \ell, \ell \matches \varnothing
}
\and
\inferrule*[lab={\ruleName{pat-neg-lit}}]
{
  \strut
}
{
  \rho, -n, -n \matches \varnothing
}
\and
\inferrule*[lab={\ruleName{pat-var}}]
{
  \strut
}
{
  \rho, x, v \matches \set{\bind{x}{v}}
}
\and
\inferrule*[lab={\ruleName{pat-wild}}]
{
  \strut
}
{
  \rho, \patWild, v \matches \varnothing
}
\and
\inferrule*[lab={\ruleName{pat-list}}]
{
  \rho, p_i, v_i \matches \rho_i \quad (\forall i)
}
{
  \rho, \patList{p_1, \ldots, p_n}, \exList{v_1, \ldots, v_n} \matches \rho_1 \disjunion \ldots \disjunion \rho_n
}
\and
\inferrule*[lab={\ruleName{pat-tuple}}]
{
  \rho, p_i, v_i \matches \rho_i \quad (\forall i)
}
{
  \rho, \patTuple{p_1, \ldots, p_n}, \exTuple{v_1, \ldots, v_n} \matches \rho_1 \disjunion \ldots \disjunion \rho_n
}
\and
\inferrule*[lab={\ruleName{pat-as}}]
{
  \rho, p, v \matches \rho'
}
{
  \rho, p\;\pyinline{as}\;x, v \matches \rho' \disjunion \set{\bind{x}{v}}
}
\and
\inferrule*[lab={\ruleName{pat-constr}}]
{
  \resolveClass(\rho, q) = (c, C) \\
  C = \clsEntry{\Delta}{q'}{\vec{z}}{c'} \\
  \fields(C) = x_1, \ldots, x_n, x_{n+1}, \ldots, x_{n+m} \\
  \rho, p_i, \rho_o(x_i) \matches \rho_i \quad (\forall i.\, 1 \le i \le n) \\
  \rho, p_{n+j}, \rho_o(y_j) \matches \rho_{n+j} \quad (\forall j.\, 1 \le j \le m)
}
{
  \rho, \patConstr{q}{p_1, \ldots, p_n, y_1 \mathord{=} p_{n+1}, \ldots, y_m \mathord{=} p_{n+m}}, \exObj{\Delta}{q'.c}{\rho_o} \matches \rho_1 \disjunion \ldots \disjunion \rho_{n+m}
}
\end{mathpar}
\caption{Pattern matching}
\label{fig:match}
\end{figure}
