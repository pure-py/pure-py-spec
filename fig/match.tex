\begin{figure}
\flushleft \shadebox{$\rho, p, v \matches \replaced{m}{\rho'}$}
\begin{mathpar}
\small
\inferrule*[lab={\ruleName{eval-pat-lit}}]
{
  \added{\eqOp(\ell, v) = \pyinline{True}}
}
{
  \rho, \ell, \replaced{v}{\ell} \matches \replaced{\matchOk{\varnothing}}{\varnothing}
}
\and
\added{\inferrule*[lab={\ruleName{eval-pat-lit-no}}]
{
  \eqOp(\ell, v) = \pyinline{False}
}
{
  \rho, \ell, v \matches \noMatch
}}
\and
\inferrule*[lab={\ruleName{eval-pat-neg-lit}}]
{
  \added{\eqOp(-n, v) = \pyinline{True}}
}
{
  \rho, -n, \replaced{v}{-n} \matches \replaced{\matchOk{\varnothing}}{\varnothing}
}
\and
\added{\inferrule*[lab={\ruleName{eval-pat-neg-lit-no}}]
{
  \eqOp(-n, v) = \pyinline{False}
}
{
  \rho, -n, v \matches \noMatch
}}
\and
\inferrule*[lab={\ruleName{eval-pat-var}}]
{
  \strut
}
{
  \rho, x, v \matches \replaced{\matchOk{\set{\bind{x}{v}}}}{\set{\bind{x}{v}}}
}
\and
\inferrule*[lab={\ruleName{eval-pat-wild}}]
{
  \strut
}
{
  \rho, \patWild, v \matches \replaced{\matchOk{\varnothing}}{\varnothing}
}
\and
\inferrule*[lab={\ruleName{eval-pat-list}}]
{
  \rho, p_i, v_i \matches \replaced{m_i}{\rho_i} \quad (\forall i)
}
{
  \rho, \patList{p_1, \ldots, p_n}, \exList{v_1, \ldots, v_n} \matches \replaced{m_1 \override \ldots \override m_n}{\rho_1 \disjunion \ldots \disjunion \rho_n}
}
\and
\added{\inferrule*[lab={\ruleName{eval-pat-list-no}}]
{
  v \text{ is neither a list nor a tuple, or is one of length} \neq n
}
{
  \rho, \patList{p_1, \ldots, p_n}, v \matches \noMatch
}}
\and
\inferrule*[lab={\ruleName{eval-pat-tuple}}]
{
  \rho, p_i, v_i \matches \replaced{m_i}{\rho_i} \quad (\forall i)
}
{
  \rho, \patTuple{p_1, \ldots, p_n}, \exTuple{v_1, \ldots, v_n} \matches \replaced{m_1 \override \ldots \override m_n}{\rho_1 \disjunion \ldots \disjunion \rho_n}
}
\and
\added{\inferrule*[lab={\ruleName{eval-pat-tuple-no}}]
{
  v \text{ is neither a list nor a tuple, or is one of length} \neq n
}
{
  \rho, \patTuple{p_1, \ldots, p_n}, v \matches \noMatch
}}
\and
\inferrule*[lab={\ruleName{eval-pat-dict}}]
{
  \bind{w_i}{\replaced{v_i}{u_i}} \in \delta \quad (\forall i) \\
  \rho, p_i, \replaced{v_i}{u_i} \matches \replaced{m_i}{\rho_i} \quad (\forall i)
}
{
  \rho, \patDict{\bind{w_1}{p_1}, \ldots, \bind{w_n}{p_n}}, \delta \matches \replaced{m_1 \override \ldots \override m_n}{\rho_1 \disjunion \ldots \disjunion \rho_n}
}
\and
\added{\inferrule*[lab={\ruleName{eval-pat-dict-no}}]
{
  v \text{ is not a dictionary, or is } \delta \text{ with } w_i \notin \dom(\delta) \text{ for some } i
}
{
  \rho, \patDict{\bind{w_1}{p_1}, \ldots, \bind{w_n}{p_n}}, v \matches \noMatch
}}
\and
\inferrule*[lab={\ruleName{eval-pat-as}}]
{
  \rho, p, v \matches \replaced{m}{\rho'}
}
{
  \rho, p\;\pyinline{as}\;x, v \matches \replaced{m \override \matchOk{\set{\bind{x}{v}}}}{\rho' \disjunion \set{\bind{x}{v}}}
}
\and
\inferrule*[lab={\ruleName{eval-pat-constr}}]
{
  \resolveClass(\rho, q) = C \\
  C \in \ancestors(C') \\
  \rho, \fieldMap(C, \seq{p})(x), \rho^\dagger(x) \matches \replaced{m_x}{\rho_x} \quad (\forall x \in \fields(C))
}
{
  \rho, \patConstr{q}{\seq{p}}, \exObj{\Gamma'}{C'}{\rho^\dagger} \matches \replaced{{\textstyle\bigotimes_{x \in \fields(C)}}\, m_x}{{\textstyle\biguplus_{x \in \fields(C)}}\, \rho_x}
}
\and
\added{\inferrule*[lab={\ruleName{eval-pat-constr-no}}]
{
  \resolveClass(\rho, q) = C \\
  v \text{ is not an object, or is } \exObj{\Gamma'}{C'}{\rho^\dagger} \text{ with } C \notin \ancestors(C')
}
{
  \rho, \patConstr{q}{\seq{p}}, v \matches \noMatch
}}
\end{mathpar}
\caption{Pattern $p$ matches value $v$ under environment $\rho$, \replaced{with result $m$}{producing bindings $\rho'$}}
\label{fig:match}
\end{figure}
