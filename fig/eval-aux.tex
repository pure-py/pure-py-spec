\begin{figure}
\small
\begin{align*}
\elems(\exList{v_1, \ldots, v_n}) &{}= v_1 \ldots v_n \\
\elems(\exTuple{v_1, \ldots, v_n}) &{}= v_1 \ldots v_n \\
\\
\outcome(\returns{v}) &= \val{v} \\
\outcome(\assigns{\rho}) &= \val{\pyinline{None}} \\
\outcome(\fails{\kappa}) &= \fails{\kappa} \\
\\
\env(\rho, x) &= \rho' && \text{where } \rho(x) = \modLoaded{q'}{\rho'} \\
\env(\rho, q.x) &= \rho' && \text{where } \env(\rho, q)(x) = \modLoaded{q'}{\rho'} \\
\\
\resolveClass(\rho, c) &= \rho(c) \\
\resolveClass(\rho, q.c) &= \env(\rho, q)(c) \\
\\
\dispatch(\rho, v, \varepsilon) &= (\varnothing, \exPass) \\
\dispatch(\rho, v, (\exCase{p}{b}) \cons \seq{c}) &= (\rho', b) && \text{if } \rho, p, v \matches \rho' \\
\dispatch(\rho, v, (\exCase{p}{b}) \cons \seq{c}) &= \dispatch(\rho, v, \seq{c}) && \text{if } \rho, p, v \notmatches \\
\\
\entries(d, \varepsilon) &{}= d \\
\entries(d, \bind{w}{v} \cons s) &{}= \entries(\update(d, w, v), s) \\
\\
\update(\varepsilon, w, v) &{}= \bind{w}{v} \\
\update(\bind{w}{u} \cons d, w, v) &{}= \bind{w}{v} \cons d \\
\update(\bind{w'}{u} \cons d, w, v) &{}= \bind{w'}{u} \cons \update(d, w, v) && \text{if } w \neq w'
\end{align*}
\caption{Metafunctions used by block and expression evaluation}
\label{fig:eval-aux}
\end{figure}
