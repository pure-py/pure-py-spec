\begin{figure}
\small
\begin{align*}
\elems(\exList{v_1, \ldots, v_n}) &{}= v_1 \cons \ldots \cons v_n \\
\elems(\exTuple{v_1, \ldots, v_n}) &{}= v_1 \cons \ldots \cons v_n \\
\elems(w) &{}= w_1 \cons \ldots \cons w_m && \text{where } w = w_1 \concat \ldots \concat w_m,\ \text{each } w_i \text{ one character} \\
\\
\iterOp(\delta) &{}= w_1 \cons \ldots \cons w_n && \text{where } \delta = \bind{w_1}{v_1} \cons \ldots \cons \bind{w_n}{v_n} \\
\iterOp(v) &{}= \elems(v) && (\text{otherwise}) \\
\\
\getitem(\delta, w) &{}= \val{\delta(w)} && \text{if } w \in \dom(\delta) \\
\getitem(\delta, w) &{}= \aborts{\errKey} && \text{if } w \notin \dom(\delta) \\
\getitem(v, n) &{}= \val{v_{n+1}} && \text{if } \elems(v) = v_1 \cons \ldots \cons v_m \text{ and } 0 \leq n < m \\
\getitem(v, n) &{}= \val{v_{m+n+1}} && \text{if } \elems(v) = v_1 \cons \ldots \cons v_m \text{ and } {-m} \leq n < 0 \\
\getitem(v, n) &{}= \aborts{\errIndex} && \text{if } \elems(v) = v_1 \cons \ldots \cons v_m \text{ and } (n < -m \text{ or } n \geq m) \\
\getitem(v, v') &{}= \aborts{\errType} && (\text{otherwise}; \; v \text{ not a dictionary}) \\
\\
\outcome(\returns{v}) &= \val{v} \\
\outcome(\assigns{\rho}) &= \val{\pyinline{None}} \\
\outcome(\aborts{\kappa}) &= \aborts{\kappa} \\
\\
\dispatch(\rho, v, \varepsilon, \varepsilon) &= (\varnothing, \exPass) \\
\dispatch(\rho, v, p \cons \seq{p}, b \cons \seq{b}) &= (\rho', b) && \text{if } \rho, p, v \matches \matchOk{}\rho' \\
\dispatch(\rho, v, p \cons \seq{p}, b \cons \seq{b}) &= \dispatch(\rho, v, \seq{p}, \seq{b}) && \text{if } \rho, p, v \matches \noMatch \\
\\
\entries(\delta, \varepsilon) &{}= \delta \\
\entries(\delta, \bind{w}{v} \cons \delta') &{}= \entries(\update(\delta, w, v), \delta') \\
\\
\update(\varepsilon, w, v) &{}= \bind{w}{v} \\
\update(\bind{w}{u} \cons \delta, w, v) &{}= \bind{w}{v} \cons \delta \\
\update(\bind{w'}{u} \cons \delta, w, v) &{}= \bind{w'}{u} \cons \update(\delta, w, v) && \text{if } w \neq w'
\end{align*}
\caption{Metafunctions used by block and expression evaluation}
\label{fig:eval-aux}
\end{figure}
