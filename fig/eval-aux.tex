\begin{figure}
\small
\begin{align*}
\elems(\exList{v_1, \ldots, v_n}) &{}= v_1 \cons \ldots \cons v_n \\
\elems(\exTuple{v_1, \ldots, v_n}) &{}= v_1 \cons \ldots \cons v_n \\
\added{\elems(w)} &{}\added{{}= w_1 \cons \ldots \cons w_m} && \added{\text{where } m \text{ is the length of } w \text{ and } w_i \text{ its } i\text{th character}} \\
\\
\added{\iterOp(\delta)} &{}\added{{}= w_1 \cons \ldots \cons w_n} && \added{\text{where } \delta = \bind{w_1}{v_1} \cons \ldots \cons \bind{w_n}{v_n}} \\
\added{\iterOp(v)} &{}\added{{}= \elems(v)} && \added{(\text{otherwise})} \\
\\
\added{\getitem(\delta, w)} &{}\added{{}= \val{\delta(w)}} && \added{\text{if } w \in \dom(\delta)} \\
\added{\getitem(\delta, w)} &{}\added{{}= \fails{\errKey}} && \added{\text{if } w \notin \dom(\delta)} \\
\added{\getitem(v, n)} &{}\added{{}= \val{v_{n+1}}} && \added{\text{if } \elems(v) = v_1 \cons \ldots \cons v_m \text{ and } 0 \leq n < m} \\
\added{\getitem(v, n)} &{}\added{{}= \val{v_{m+n+1}}} && \added{\text{if } \elems(v) = v_1 \cons \ldots \cons v_m \text{ and } {-m} \leq n < 0} \\
\added{\getitem(v, n)} &{}\added{{}= \fails{\errIndex}} && \added{\text{if } \elems(v) = v_1 \cons \ldots \cons v_m \text{ and } (n < -m \text{ or } n \geq m)} \\
\added{\getitem(v, v')} &{}\added{{}= \fails{\errType}} && \added{(\text{otherwise}; \; v \text{ not a dictionary})} \\
\\
\outcome(\returns{v}) &= \val{v} \\
\outcome(\assigns{\rho}) &= \val{\pyinline{None}} \\
\outcome(\fails{\kappa}) &= \fails{\kappa} \\
\\
\env(\rho, x) &= \rho' && \text{where } \rho(x) = \modLoaded{q'}{\rho'} \\
\env(\rho, q.x) &= \rho' && \text{where } \env(\rho, q)(x) = \modLoaded{q'}{\rho'} \\
\\
\resolveClass(\rho, c) &= \rho(c) \\
\resolveClass(\rho, q.c) &= \env(\rho, q)(c) \\
\\
\dispatch(\rho, v, \varepsilon, \varepsilon) &= (\varnothing, \exPass) \\
\dispatch(\rho, v, p \cons \seq{p}, b \cons \seq{b}) &= (\rho', b) && \text{if } \rho, p, v \matches \rho' \\
\dispatch(\rho, v, p \cons \seq{p}, b \cons \seq{b}) &= \dispatch(\rho, v, \seq{p}, \seq{b}) && \text{if } \rho, p, v \notmatches \\
\\
\entries(\delta, \varepsilon) &{}= \delta \\
\entries(\delta, \bind{w}{v} \cons \delta') &{}= \entries(\update(\delta, w, v), \delta') \\
\\
\update(\varepsilon, w, v) &{}= \bind{w}{v} \\
\update(\bind{w}{u} \cons \delta, w, v) &{}= \bind{w}{v} \cons \delta \\
\update(\bind{w'}{u} \cons \delta, w, v) &{}= \bind{w'}{u} \cons \update(\delta, w, v) && \text{if } w \neq w'
\end{align*}
\caption{Metafunctions used by block and expression evaluation}
\label{fig:eval-aux}
\end{figure}
