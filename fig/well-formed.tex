\begin{figure}
\begin{subfigure}{\textwidth}
\flushleft \shadebox{$\Gamma \vdash s : R$}
\begin{mathpar}
\small
\inferrule*[lab={\ruleName{pass}}]
{
  \strut
}
{
  \Gamma \vdash \exPass : \Assigns{\varnothing}
}
\and
\inferrule*[lab={\ruleName{assign}}]
{
  \Gamma \vdash e \\
  x \notin \mathrm{captures}(e)
}
{
  \Gamma \vdash \exAssign{x}{e} : \Assigns{\set{x : \statusDefAssigned}}
}
\and
\inferrule*[lab={\ruleName{return-none}}]
{
  \strut
}
{
  \Gamma \vdash \exReturnNone : \Returns
}
\and
\inferrule*[lab={\ruleName{return}}]
{
  \Gamma \vdash e
}
{
  \Gamma \vdash \exReturn{e} : \Returns
}
\and
\inferrule*[lab={\ruleName{if}}]
{
  \Gamma \vdash \vec{e} \\
  \Gamma \vdash \seq{s}_1 : R_1 \;\ldots\; \Gamma \vdash \seq{s}_{n+1} : R_{n+1}
}
{
  \Gamma \vdash \exIf{e_1}{\seq{s}_1}{\exElif{e_2}{\seq{s}_2}\;\ldots\;\exElif{e_{n+1}}{\seq{s}_{n+1}}} : R_1 \merge \ldots R_{n + 1} \merge \ldots \merge \Assigns{\varnothing}
}
\and
\inferrule*[lab={\ruleName{if-else}}]
{
  \Gamma \vdash \vec{e} \\
  \Gamma \vdash \seq{s}_1 : R_1 \;\ldots\; \Gamma \vdash \seq{s}_{n+2} : R_{n+2}
}
{
  \Gamma \vdash \exIfElse{e_1}{\seq{s}_1}{\exElif{e_2}{\seq{s}_2}\;\ldots\;\exElif{e_{n+1}}{\seq{s}_{n+1}}}{\seq{s}_{n+2}} : R_1 \merge \ldots \merge R_{n+2}
}
\and
\inferrule*[lab={\ruleName{mutual}}]
{
  \Delta = \set{\seq{f : \statusDefAssigned}} \\
  \Gamma \runion \Delta, x_{1,1} : \statusDefAssigned, \ldots, x_{1,k_1} : \statusDefAssigned, \seq{\assigns(\seq{s}_1) : \statusNotDefAssigned} \vdash \seq{s}_1 : R_1
   \quad\ldots\quad
   \Gamma \runion \Delta, x_{n,1} : \statusDefAssigned, \ldots, x_{n,k_n} : \statusDefAssigned, \assigns(\seq{s}_n) : \statusNotDefAssigned \vdash \seq{s}_n : R_n \\
  f_1, \ldots, f_n \text{ distinct}
}
{
  \Gamma \vdash \exDef{f_1}{\vec{x}_1}{\seq{s}_1} \cons \ldots \cons \exDef{f_n}{\vec{x}_n}{\seq{s}_n} : \Assigns{\Delta}
}
\end{mathpar}
\caption{Statement $s$ is well-formed in $\Gamma$}
\end{subfigure}
\begin{subfigure}{\textwidth}
\flushleft \shadebox{$\Gamma \vdash \seq{s} : R$}
\begin{mathpar}
\small
\inferrule*[lab={\ruleName{empty}}]
{
  \strut
}
{
  \Gamma \vdash \varepsilon : \Assigns{\varnothing}
}
\and
\inferrule*[lab={\ruleName{cons}}]
{
  \Gamma \vdash s : \Assigns{\Delta} \\
  \Gamma \runion \Delta \vdash \seq{s}\,' : R \\
  \mathrm{captures}(s) \cap \assigns(\seq{s}\,') = \varnothing \\
  \neg(s \text{ is a mutual block} \wedge \seq{s}\,' \text{ begins with a mutual block})
}
{
  \Gamma \vdash s \cons \seq{s}\,' : \Assigns{\Delta} \runion R
}
\end{mathpar}
\caption{Statement sequence $\seq{s}$ is well-formed in $\Gamma$}
\end{subfigure}
\begin{subfigure}{\textwidth}
\flushleft \shadebox{$\wf{\Gamma}{e}$}
\begin{mathpar}
\small
\inferrule*[right=var]
{
  \Gamma(x) = \statusDefAssigned
}
{
  \wf{\Gamma}{x}
}
\and
\inferrule*[right=lambda]
{
  \Gamma, x_1 : \statusDefAssigned, \ldots, x_n : \statusDefAssigned \vdash e
}
{
  \wf{\Gamma}{\exLambda{\vec{x}}{e}}
}
\end{mathpar}
\caption{Expression $e$ is well-formed in $\Gamma$}
\end{subfigure}
\caption{Well-formedness rules}
\end{figure}
