\begin{figure}
\begin{subfigure}{\textwidth}
\flushleft \shadebox{$\Gamma \vdash s : R$}
\begin{mathpar}
\small
\inferrule*[lab={\ruleName{pass}}]
{
  \strut
}
{
  \Gamma \vdash \exPass : \Assigns{\varnothing}
}
\and
\inferrule*[lab={\ruleName{assign}}]
{
  \Gamma \vdash e \\
  x \notin \mathrm{captures}(e)
}
{
  \Gamma \vdash \exAssign{x}{e} : \Assigns{\set{x : \statusDefAssigned}}
}
\and
\inferrule*[lab={\ruleName{return-none}}]
{
  \strut
}
{
  \Gamma \vdash \exReturnNone : \Returns
}
\and
\inferrule*[lab={\ruleName{return}}]
{
  \Gamma \vdash e
}
{
  \Gamma \vdash \exReturn{e} : \Returns
}
\and
\inferrule*[lab={\ruleName{if}}]
{
  \Gamma \vdash \vec{e} \\
  \Gamma \vdash \seq{s}_1 : R_1 \quad\ldots\quad \Gamma \vdash \seq{s}_{n+1} : R_{n+1}
}
{
  \Gamma \vdash \exIf{e_1}{\seq{s}_1}{\exElif{e_2}{\seq{s}_2}\;\ldots\;\exElif{e_{n+1}}{\seq{s}_{n+1}}} : R_1 \merge \ldots R_{n + 1} \merge \ldots \merge \Assigns{\varnothing}
}
\and
\inferrule*[lab={\ruleName{if-else}}]
{
  \Gamma \vdash \vec{e} \\
  \Gamma \vdash \seq{s}_1 : R_1 \quad\ldots\quad \Gamma \vdash \seq{s}_{n+2} : R_{n+2}
}
{
  \Gamma \vdash \exIfElse{e_1}{\seq{s}_1}{\exElif{e_2}{\seq{s}_2}\;\ldots\;\exElif{e_{n+1}}{\seq{s}_{n+1}}}{\seq{s}_{n+2}} : R_1 \merge \ldots \merge R_{n+2}
}
\and
\inferrule*[lab={\ruleName{mutual}}]
{
  \Gamma \runion \statusDefAssigned(\seq{f}) \runion \statusDefAssigned(\seq{x}_{1}) \runion
  \statusNotDefAssigned(\assignsF(\seq{s}_1) \setminus \seq{x}_1) \vdash \seq{s}_1 : R_1
   \quad\ldots\quad
   \Gamma \runion \statusDefAssigned(\seq{f}) \runion \statusDefAssigned(\seq{x}_{n}) \runion
   \statusNotDefAssigned(\assignsF(\seq{s}_n) \setminus \seq{x}_n) \vdash \seq{s}_n : R_n \\
  f_1, \ldots, f_n \text{ distinct}
}
{
  \Gamma \vdash \exDef{f_1}{\vec{x}_1}{\seq{s}_1} \cons \ldots \cons \exDef{f_n}{\vec{x}_n}{\seq{s}_n} : \Assigns{\statusDefAssigned(\seq{f})}
}
\end{mathpar}
\caption{Statement $s$ is well-formed in $\Gamma$}
\end{subfigure}
\begin{subfigure}{\textwidth}
\flushleft \shadebox{$\Gamma \vdash \seq{s} : R$}
\begin{mathpar}
\small
\inferrule*[lab={\ruleName{empty}}]
{
  \strut
}
{
  \Gamma \vdash \varepsilon : \Assigns{\varnothing}
}
\and
\inferrule*[lab={\ruleName{cons}}]
{
  \Gamma \vdash t : \Assigns{\Delta} \\
  \Gamma \runion \Delta \vdash \seq{s} : R \\
  \mathrm{captures}(t) \cap \assignsF(\seq{s}) = \varnothing
}
{
  \Gamma \vdash t \cons \seq{s} : \Assigns{\Delta} \runion R
}
\and
\inferrule*[lab={\ruleName{cons-block-final}}]
{
  \Gamma \vdash \seq{d} : \Assigns{\Delta}
}
{
  \Gamma \vdash \seq{d} \cons \varepsilon : \Assigns{\Delta}
}
\and
\inferrule*[lab={\ruleName{cons-block-cont}}]
{
  \Gamma \vdash \seq{d} : \Assigns{\Delta} \\
  \Gamma \runion \Delta \vdash t \cons \seq{s} : R \\
  \mathrm{captures}(\seq{d}) \cap \assignsF(t \cons \seq{s}) = \varnothing
}
{
  \Gamma \vdash \seq{d} \cons t \cons \seq{s} : \Assigns{\Delta} \runion R
}
\end{mathpar}
\caption{Statement sequence $\seq{s}$ is well-formed in $\Gamma$}
\end{subfigure}
\begin{subfigure}{\textwidth}
\flushleft \shadebox{$\wf{\Gamma}{e}$}
\begin{mathpar}
\small
\inferrule*[right=var]
{
  \Gamma(x) = \statusDefAssigned
}
{
  \wf{\Gamma}{x}
}
\and
\inferrule*[right=lambda]
{
  \Gamma, x_1 : \statusDefAssigned, \ldots, x_n : \statusDefAssigned \vdash e
}
{
  \wf{\Gamma}{\exLambda{\vec{x}}{e}}
}
\end{mathpar}
\caption{Expression $e$ is well-formed in $\Gamma$}
\end{subfigure}
\caption{Well-formedness rules}
\end{figure}
