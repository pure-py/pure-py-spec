\figref{mutable-state} lists the Python constructs that update a value in place or rebind a variable, and
the behaviours that depend on such a change, and says how and when \PurePy excludes each.

\input{spec/fig/mutable-state}
