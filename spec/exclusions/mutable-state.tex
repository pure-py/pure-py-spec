\figref{mutable-state} lists the constructs that mutate or rebind state, and the behaviours that depend on
such mutation, with the stage at which each exclusion takes effect.

\input{spec/fig/mutable-state}
