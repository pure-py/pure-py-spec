\begin{figure}
\flushleft \shadebox{$\Gamma; \zeta \vdash t : R$}
\begin{mathpar}
\small
\inferrule*[lab={\ruleName{pass}}]
{
  \strut
}
{
  \Gamma; \zeta \vdash \exPass : \Assigns{\varnothing}
}
\and
\inferrule*[lab={\ruleName{assign}}]
{
  \Gamma \vdash \psi \synth \sigma \\
  \Gamma \vdash e \checks \sigma \\
  x \notin \capturesE(e)
}
{
  \Gamma; \zeta \vdash \exAssign{x \exAnnot{\psi}}{e} : \Assigns{\set{\bind{x}{\sigma}}}
}
\and
\inferrule*[lab={\ruleName{assign-unannot}}]
{
  \Gamma \vdash e \synth \sigma \\
  x \notin \capturesE(e)
}
{
  \Gamma; \zeta \vdash \exAssign{x}{e} : \Assigns{\set{\bind{x}{\sigma}}}
}
\and
\inferrule*[lab={\ruleName{expr-stmt}}]
{
  \Gamma \vdash e \synth \sigma
}
{
  \Gamma; \zeta \vdash e : \Assigns{\varnothing}
}
\and
\inferrule*[lab={\ruleName{assert}}]
{
  \Gamma \vdash e \checks \tyBool
}
{
  \Gamma; \zeta \vdash \exAssert{e} : \Assigns{\varnothing}
}
\and
\inferrule*[lab={\ruleName{assert-msg}}]
{
  \Gamma \vdash e \checks \tyBool \\
  \Gamma \vdash e' \checks \tyStr
}
{
  \Gamma; \zeta \vdash \exAssertMsg{e}{e'} : \Assigns{\varnothing}
}
\and
\inferrule*[lab={\ruleName{return-none}}]
{
  \Gamma \vdash \tyNone \subty \tau
}
{
  \Gamma; \tau \vdash \exReturnNone : \Returns
}
\and
\inferrule*[lab={\ruleName{return}}]
{
  \Gamma \vdash e \checks \tau
}
{
  \Gamma; \tau \vdash \exReturn{e} : \Returns
}
\and
\inferrule*[lab={\ruleName{if}}]
{
  \Gamma \vdash e_i \checks \tyBool \quad (\forall i) \\
  \Gamma; \zeta \vdash s_i : R_i \quad (\forall i)
}
{
  \Gamma; \zeta \vdash \exIf{e_1}{s_1}{\exElif{e_2}{s_2}\;\ldots\;\exElif{e_{n+1}}{s_{n+1}}} : R_1 \merge \ldots \mathbin{\merge} R_{n+1} \: \merge \Assigns{\varnothing}
}
\and
\inferrule*[lab={\ruleName{if-else}}]
{
  \Gamma \vdash e_i \checks \tyBool \quad (\forall i) \\
  \Gamma; \zeta \vdash s_i : R_i \quad (\forall i)
}
{
  \Gamma; \zeta \vdash \exIfElse{e_1}{s_1}{\exElif{e_2}{s_2}\;\ldots\;\exElif{e_{n+1}}{s_{n+1}}}{s_{n+2}} : R_1 \merge \ldots \merge R_{n+2}
}
\and
\inferrule*[lab={\ruleName{seq}}]
{
  \Gamma; \zeta \vdash t : \Assigns{\Delta} \\
  \Gamma \override \Delta ; \tau \vdash t' : R \\
  \capturesS(t) \cap \assignsS(t') = \varnothing
}
{
  \Gamma; \zeta \vdash t\;t' : \Assigns{\Delta} \override R
}
\and
\inferrule*[lab={\ruleName{def}}]
{
  \Delta_i = \set{\bind{\seq{x}_i}{\seq{\sigma}_i}} \quad (\forall i) \\
  \Gamma \override \set{\bind{f_j}{\tyCallable{\seq{\sigma}_j}{\tau_j}} \mid \forall j} \override \Delta_i \override \statusNotDefAssigned(\assignsS(s_i) \setminus \dom(\Delta_i)); \tau_i \vdash s_i : R_i \quad (\forall i)
  \\
  R_i = \Returns \text{ or } \Gamma \vdash \tyNone \subty \tau_i \quad (\forall i) \\
  \seq{f} \text{ distinct} \\
  \Gamma \vdash \seq{\psi}_i \synth \seq{\sigma}_i \quad (\forall i) \\
  \Gamma \vdash \psi_i \synth \tau_i \quad (\forall i)
}
{
  \Gamma; \zeta \vdash \exDef{f_1}{\bind{\seq{x}_1}{\seq{\psi}_1}}{\exRetAnnot{\psi_1}}{s_1} \cons \ldots \cons \exDef{f_{n+1}}{\bind{\seq{x}_{n+1}}{\seq{\psi}_{n+1}}}{\exRetAnnot{\psi_{n+1}}}{s_{n+1}} : \Assigns{\set{\bind{f_j}{\tyCallable{\seq{\sigma}_j}{\tau_j}} \mid \forall j}}
}
\and
\inferrule*[lab={\ruleName{match}}]
{
  \Gamma \vdash e \synth \sigma \\
  \Gamma; \zeta; \shapesOf(\widen(\sigma), \varnothing) \vdash \seq{h} : S, \varnothing
}
{
  \Gamma; \zeta \vdash \exMatch{e}{\seq{h}} : S
}
\and
\inferrule*[lab={\ruleName{match-partial}}]
{
  \Gamma \vdash e \synth \sigma \\
  \Gamma; \zeta; \shapesOf(\widen(\sigma), \varnothing) \vdash \seq{h} : S, L \\
  L \neq \varnothing
}
{
  \Gamma; \zeta \vdash \exMatch{e}{\seq{h}} : S \merge \Assigns{\varnothing}
}
\and
\inferrule*[lab={\ruleName{class}}]
{
  \nexists C'.\ \bind{c}{C'} \in \Gamma \\
  \bind{\pyinline{dataclass}}{\predefName} \in \Gamma \\
  \ownFields(\phi) = \bind{\seq{x}}{\seq{\psi}}, \; \seq{x} \text{ distinct} \\
  \Gamma \vdash \psi_i \synth \sigma_i \quad (\forall i) \\
  C = \clsEntry{\Gamma}{q.c}{\bind{\seq{x}}{\seq{\sigma}}}{\bot}
}
{
  \Gamma; \zeta \vdash \exDataclass{c}{\phi} : \Assigns{\set{\bind{c}{C}}}
}
\and
\inferrule*[lab={\ruleName{class-extend}}]
{
  \nexists C'.\ \bind{c}{C'} \in \Gamma \\
  \bind{c'}{\clsEntry{\Gamma'}{q.c'}{\seq{y}}{c^\dagger}} \in \Gamma \\
  \bind{\pyinline{dataclass}}{\predefName} \in \Gamma \\
  \ownFields(\phi) = \bind{\seq{x}}{\seq{\psi}}, \; \seq{x} \text{ distinct} \\
  \Gamma \vdash \psi_i \synth \sigma_i \quad (\forall i) \\
  \seq{x} \cap \dom(\fields(\Gamma(c'))) = \varnothing \\
  C = \clsEntry{\Gamma}{q.c}{\bind{\seq{x}}{\seq{\sigma}}}{c'}
}
{
  \Gamma; \zeta \vdash \exDataclassExtend{c}{c'}{\phi} : \Assigns{\set{\bind{c}{C}}}
}
\end{mathpar}
\caption{Statement $t$ has result type $R$ in $\Gamma$, with optional return type $\zeta$}
\label{fig:typing-stmt}
\end{figure}
