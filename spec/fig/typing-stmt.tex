\begin{figure}
\flushleft \shadebox{$\Gamma; \tau \vdash t : R$}
\begin{mathpar}
\small
\inferrule*[lab={\ruleName{ty-pass}}]
{
  \strut
}
{
  \Gamma; \tau \vdash \exPass : \Assigns{\varnothing}
}
\and
\inferrule*[lab={\ruleName{ty-assign}}]
{
  \Gamma \vdash e \checks \sigma \\
  x \notin \capturesE(e)
}
{
  \Gamma; \tau \vdash \exAssign{x \exAnnot{\sigma}}{e} : \Assigns{\set{\bind{x}{\sigma}}}
}
\and
\inferrule*[lab={\ruleName{ty-expr-stmt}}]
{
  \Gamma \vdash e \synth \sigma
}
{
  \Gamma; \tau \vdash e : \Assigns{\varnothing}
}
\and
\inferrule*[lab={\ruleName{ty-assert}}]
{
  \Gamma \vdash e \checks \tyBool
}
{
  \Gamma; \tau \vdash \exAssert{e} : \Assigns{\varnothing}
}
\and
\inferrule*[lab={\ruleName{ty-assert-msg}}]
{
  \Gamma \vdash e \checks \tyBool \\
  \Gamma \vdash e' \checks \tyStr
}
{
  \Gamma; \tau \vdash \exAssertMsg{e}{e'} : \Assigns{\varnothing}
}
\and
\inferrule*[lab={\ruleName{ty-return-none}}]
{
  \tyNone \subty \tau
}
{
  \Gamma; \tau \vdash \exReturnNone : \Returns
}
\and
\inferrule*[lab={\ruleName{ty-return}}]
{
  \Gamma \vdash e \checks \tau
}
{
  \Gamma; \tau \vdash \exReturn{e} : \Returns
}
\and
\inferrule*[lab={\ruleName{ty-if}}]
{
  \Gamma \vdash e_i \checks \tyBool \quad (\forall i) \\
  \Gamma; \tau \vdash s_i : R_i \quad (\forall i)
}
{
  \Gamma; \tau \vdash \exIf{e_1}{s_1}{\exElif{e_2}{s_2}\;\ldots\;\exElif{e_{n+1}}{s_{n+1}}} : R_1 \merge \ldots \mathbin{\merge} R_{n+1} \: \merge \Assigns{\varnothing}
}
\and
\inferrule*[lab={\ruleName{ty-if-else}}]
{
  \Gamma \vdash e_i \checks \tyBool \quad (\forall i) \\
  \Gamma; \tau \vdash s_i : R_i \quad (\forall i)
}
{
  \Gamma; \tau \vdash \exIfElse{e_1}{s_1}{\exElif{e_2}{s_2}\;\ldots\;\exElif{e_{n+1}}{s_{n+1}}}{s_{n+2}} : R_1 \merge \ldots \merge R_{n+2}
}
\and
\inferrule*[lab={\ruleName{ty-seq}}]
{
  \Gamma; \tau \vdash t : \Assigns{\Delta} \\
  \Gamma \override \Delta ; \tau \vdash t' : R \\
  \capturesS(t) \cap \assignsS(t') = \varnothing
}
{
  \Gamma; \tau \vdash t\;t' : \Assigns{\Delta} \override R
}
\and
\inferrule*[lab={\ruleName{ty-def}}]
{
  \Delta_i = \set{\bind{x_{i1}}{\tau_{i1}}, \ldots, \bind{x_{in_i}}{\tau_{in_i}}} \quad (\forall i) \\
  \Gamma \override \set{\bind{f_j}{\tyCallable{\seq{\tau}_j}{\tau_j}} \mid \forall j} \override \Delta_i \override \statusNotDefAssigned(\assignsS(s_i) \setminus \dom(\Delta_i)); \tau_i \vdash s_i : R_i \quad (\forall i)
  \\
  R_i = \Returns \text{ or } \tau_i = \tyNone \quad (\forall i) \\
  \seq{f} \text{ distinct}
}
{
  \Gamma; \tau \vdash \exDef{f_1}{\seq{x}_1\,\seq{\pi}_1}{\omega_1}{s_1} \cons \ldots \cons \exDef{f_{n+1}}{\seq{x}_{n+1}\,\seq{\pi}_{n+1}}{\omega_{n+1}}{s_{n+1}} : \Assigns{\set{\bind{f_j}{\tyCallable{\seq{\tau}_j}{\tau_j}} \mid \forall j}}
}
\and
\inferrule*[lab={\ruleName{ty-match}}]
{
  \Gamma \vdash e \synth \sigma \\
  \Gamma \vdash p_i \checks \sigma \binding \Delta_i \quad (\forall i) \\
  \Gamma \override \Delta_i ; \tau \vdash s_i : R_i \quad (\forall i) \\
  S_i = \Assigns{\Delta_i} \override R_i \quad (\forall i) \\
  p_{n+1} \in \set{x, \patWild}
}
{
  \Gamma; \tau \vdash \exMatch{e}{\exCase{p_1}{s_1}\;\ldots\;\exCase{p_{n+1}}{s_{n+1}}} : S_1 \merge \ldots \merge S_{n+1}
}
\and
\inferrule*[lab={\ruleName{ty-match-partial}}]
{
  \Gamma \vdash e \synth \sigma \\
  \Gamma \vdash p_i \checks \sigma \binding \Delta_i \quad (\forall i) \\
  \Gamma \override \Delta_i ; \tau \vdash s_i : R_i \quad (\forall i) \\
  S_i = \Assigns{\Delta_i} \override R_i \quad (\forall i) \\
  p_{n+1} \notin \set{x, \patWild}
}
{
  \Gamma; \tau \vdash \exMatch{e}{\exCase{p_1}{s_1}\;\ldots\;\exCase{p_{n+1}}{s_{n+1}}} : S_1 \merge \ldots \merge S_{n+1} \merge \Assigns{\varnothing}
}
\end{mathpar}
\caption{Statement $t$ is well-formed in $\Gamma$ with result type $R$, in a function returning $\tau$}
\label{fig:typing-stmt}
\end{figure}
