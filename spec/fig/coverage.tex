\begin{figure}
\small
\setlength{\jot}{2pt}
\begin{subfigure}{\textwidth}
\flushleft \shadebox{$\Gamma \vdash k : \tau$}
\begin{mathpar}
\small
\inferrule*[lab={\ruleName{shape-rest}}]
{
  \tau \ctors h \quad (\forall h \in N) \\
  \tau \text{ not a union}
}
{
  \Gamma \vdash \shapeAny{\tau}{N} : \tau
}
\and
\inferrule*[lab={\ruleName{shape-literal}}]
{
  \Gamma \vdash \tyLiteral{\ell} \subty \tau
}
{
  \Gamma \vdash \ell : \tau
}
\and
\inferrule*[lab={\ruleName{shape-constr}}]
{
  \Gamma \vdash q : C \\
  \Gamma \vdash q \subty \tau \\
  \Gamma \vdash k_x : \sigma \quad (\forall \bind{x}{\sigma} \in \fields(C))
}
{
  \Gamma \vdash \patConstr{q}{\seq{k}} : \tau
}
\and
\inferrule*[lab={\ruleName{shape-tuple}}]
{
  \Gamma \vdash k_i : \tau_i \quad (\forall i)
}
{
  \Gamma \vdash \patTuple{\seq{k}} : \tyTuple{\seq{\tau}}
}
\and
\inferrule*[lab={\ruleName{shape-list}}]
{
  \Gamma \vdash k_i : \tau \quad (\forall i)
}
{
  \Gamma \vdash \patList{\seq{k}} : \tyList{\tau}
}
\end{mathpar}
\subcaption{Shape $k$ denotes values of type $\tau$}
\end{subfigure}

\vspace{2mm}

\begin{subfigure}{\textwidth}
\begin{defalign}
q \ctors q' \quad & \text{iff } \Gamma \vdash q' \subty q \\
\tyList{\tau} \ctors n \quad & \text{iff } n \geq 0 \\
\tyDict{\tau} \ctors \set{w_1, \ldots, w_n} \quad & \\
\tau \ctors \ell \quad & \text{iff } \Gamma \vdash \tyLiteral{\ell} \subty \tau &\quad& \text{otherwise} \\[3mm]
\restOf(\tyUnion{\sigma}{\tau}, N) & = \restOf(\sigma, N) \cup \restOf(\tau, N) \\
\restOf(\tyLiteral{\ell}, N) & = \varnothing &\quad& \text{if } \ell \in N \\
\restOf(\tyBool, N) & = \varnothing &\quad& \text{if } \set{\pyinline{True}, \pyinline{False}} \subseteq N \\
\restOf(\tyNone, N) & = \varnothing &\quad& \text{if } \pyinline{None} \in N \\
\restOf(q, N) & = \varnothing &\quad& \text{if } \Gamma \vdash q \subty q' \text{ for some } q' \in N \\
\restOf(\tyDict{\tau}, N) & = \varnothing &\quad& \text{if } \varnothing \in N \\
\restOf(\tyNever, N) & = \varnothing \\
\restOf(\tau, N) & = \set{\shapeAny{\tau}{N}} &\quad& \text{otherwise}
\end{defalign}
\subcaption{Constructs of type $\tau$, and shapes of $\tau$ with constructs $N$ excluded}
\end{subfigure}

\caption{Shapes}
\label{fig:shapes}
\end{figure}

\begin{figure}
\small
\setlength{\jot}{2pt}
\flushleft \shadebox{$\Gamma \vdash k, p \splitTo M, K$}
\begin{mathpar}
\small
\inferrule*[lab={\ruleName{split-var}}]
{
  \strut
}
{
  \Gamma \vdash k, x \splitTo \set{k}, \varnothing
}
\and
\inferrule*[lab={\ruleName{split-wild}}]
{
  \strut
}
{
  \Gamma \vdash k, \patWild \splitTo \set{k}, \varnothing
}
\and
\inferrule*[lab={\ruleName{split-as}}]
{
  \Gamma \vdash k, p \splitTo M, K
}
{
  \Gamma \vdash k, p\;\pyinline{as}\;x \splitTo M, K
}
\and
\inferrule*[lab={\ruleName{split-literal}}]
{
  \strut
}
{
  \Gamma \vdash \ell, \ell \splitTo \set{\ell}, \varnothing
}
\and
\inferrule*[lab={\ruleName{split-rest-literal}}]
{
  \ell \notin N \\
  \Gamma \vdash \tyLiteral{\ell} \subty \tau
}
{
  \Gamma \vdash \shapeAny{\tau}{N}, \ell \splitTo \set{\ell}, \restOf(\tau, N \cup \set{\ell})
}
\and
\inferrule*[lab={\ruleName{split-rest-constr}}]
{
  q \notin N \\
  \tau, q \text{ comparable} \\
  \Gamma \vdash q : C \\
  \fields(C) = \bind{\seq{x}}{\seq{\sigma}} \\
  \Gamma \vdash (\shapeAny{\sigma_1}{\varnothing}, \ldots, \shapeAny{\sigma_n}{\varnothing}), a \splitTo M, K
}
{
  \Gamma \vdash \shapeAny{\tau}{N}, \patConstr{q}{a} \splitTo \set{\patConstr{q}{\seq{m}} \mid \seq{m} \in M}, \set{\patConstr{q}{\seq{k}} \mid \seq{k} \in K} \cup \restOf(\tau, N \cup \set{q})
}
\and
\inferrule*[lab={\ruleName{split-rest-tuple}}]
{
  \Gamma \vdash (\shapeAny{\tau_1}{\varnothing}, \ldots, \shapeAny{\tau_n}{\varnothing}), \seq{p} \splitTo M, K
}
{
  \Gamma \vdash \shapeAny{\tyTuple{\tau_1, \ldots, \tau_n}}{N}, \patTuple{p_1, \ldots, p_n} \splitTo \set{\patTuple{\seq{m}} \mid \seq{m} \in M}, \set{\patTuple{\seq{k}} \mid \seq{k} \in K}
}
\and
\inferrule*[lab={\ruleName{split-rest-list}}]
{
  n \notin N \\
  \Gamma \vdash (\shapeAny{\tau}{\varnothing}, \ldots, \shapeAny{\tau}{\varnothing}), p_1 \cons \ldots \cons p_n \splitTo M, K
}
{
  \Gamma \vdash \shapeAny{\tyList{\tau}}{N}, \patList{p_1, \ldots, p_n} \splitTo \set{\patList{\seq{m}} \mid \seq{m} \in M}, \set{\patList{\seq{k}} \mid \seq{k} \in K} \cup \restOf(\tyList{\tau}, N \cup \set{n})
}
\and
\inferrule*[lab={\ruleName{split-constr}}]
{
  \Gamma \vdash q \subty q' \\
  \Gamma \vdash \seq{k}, a \splitTo M, K
}
{
  \Gamma \vdash \patConstr{q}{\seq{k}}, \patConstr{q'}{a} \splitTo \set{\patConstr{q}{\seq{m}} \mid \seq{m} \in M}, \set{\patConstr{q}{\seq{k}\,'} \mid \seq{k}\,' \in K}
}
\and
\inferrule*[lab={\ruleName{split-tuple}}]
{
  \Gamma \vdash \seq{k}, \seq{p} \splitTo M, K
}
{
  \Gamma \vdash \patTuple{k_1, \ldots, k_n}, \patTuple{p_1, \ldots, p_n} \splitTo \set{\patTuple{\seq{m}} \mid \seq{m} \in M}, \set{\patTuple{\seq{k}\,'} \mid \seq{k}\,' \in K}
}
\and
\inferrule*[lab={\ruleName{split-list}}]
{
  \Gamma \vdash \seq{k}, \seq{p} \splitTo M, K
}
{
  \Gamma \vdash \patList{k_1, \ldots, k_n}, \patList{p_1, \ldots, p_n} \splitTo \set{\patList{\seq{m}} \mid \seq{m} \in M}, \set{\patList{\seq{k}\,'} \mid \seq{k}\,' \in K}
}
\and
\inferrule*[lab={\ruleName{split-rest-dict}}]
{
  N' \not\subseteq \set{w_1, \ldots, w_n} \quad (\forall N' \in N) \\
  \Gamma \vdash \restOf(\tau, \varnothing), p_i \splitTo M_i, \varnothing \quad (\forall i)
}
{
  \Gamma \vdash \shapeAny{\tyDict{\tau}}{N}, \patDict{\bind{w_1}{p_1}, \ldots, \bind{w_n}{p_n}} \splitTo \set{\shapeAny{\tyDict{\tau}}{N}}, \restOf(\tyDict{\tau}, N \cup \set{\set{w_1, \ldots, w_n}})
}
\and
\inferrule*[lab={\ruleName{split-rest-dict-partial}}]
{
  N' \not\subseteq \set{w_1, \ldots, w_n} \quad (\forall N' \in N) \\
  \Gamma \vdash \restOf(\tau, \varnothing), p_i \splitTo M_i, K_i \quad (\forall i) \\
  K_j \neq \varnothing \quad (\text{some } j)
}
{
  \Gamma \vdash \shapeAny{\tyDict{\tau}}{N}, \patDict{\bind{w_1}{p_1}, \ldots, \bind{w_n}{p_n}} \splitTo \set{\shapeAny{\tyDict{\tau}}{N}}, \set{\shapeAny{\tyDict{\tau}}{N}}
}
\and
\inferrule*[lab={\ruleName{split-none}}]
{
  \text{no other rule applies}
}
{
  \Gamma \vdash k, p \splitTo \varnothing, \set{k}
}
\end{mathpar}
\caption{Pattern $p$ splits shape $k$ into matched shapes $M$ and residual $K$}
\label{fig:coverage}
\end{figure}

\begin{figure}
\begin{mathpar}
\small
\inferrule*[lab={\ruleName{split-row}}]
{
  \Gamma \vdash k_i, p_i \splitTo M_i, K_i \quad (\forall i)
}
{
  \Gamma \vdash (k_1, \ldots, k_n), (p_1, \ldots, p_n) \splitTo \set{(m_1, \ldots, m_n) \mid m_i \in M_i}, \; \textstyle\bigcup_i \set{(m_1, \ldots, m_{i-1}, k, k_{i+1}, \ldots, k_n) \mid k \in K_i, \; m_j \in M_j}
}
\and
\inferrule*[lab={\ruleName{split-shapes}}]
{
  \Gamma \vdash k, p \splitTo M_k, K_k \quad (\forall k \in K)
}
{
  \Gamma \vdash K, p \splitTo \textstyle\bigcup_{k \in K} M_k, \; \textstyle\bigcup_{k \in K} K_k
}
\end{mathpar}
\caption{Splitting rows and sets of shapes}
\label{fig:coverage-rows}
\end{figure}
