\begin{figure}
\flushleft \shadebox{$\Sigma \vdash \jointSub{\gty{\tau}}{\gty{\sigma}}{\gty{\tau}'}$}
\begin{mathpar}
\small
\inferrule*[lab={\ruleName{joint-sub-any}}]
{
  \strut
}
{
  \Sigma \vdash \jointSub{\gty{\tau}}{\tyAny}{\gty{\sigma}}
}
\and
\inferrule*[lab={\ruleName{joint-sub-any-left}}]
{
  \Sigma \vdash \gty{\tau} \gsubty \gty{\sigma}
}
{
  \Sigma \vdash \jointSub{\tyAny}{\gty{\sigma}}{\gty{\tau}}
}
\and
\inferrule*[lab={\ruleName{joint-sub-never-left}}]
{
  \Sigma \vdash \gty{\tau} \gsubty \gty{\sigma}
}
{
  \Sigma \vdash \jointSub{\tyNever}{\gty{\sigma}}{\gty{\tau}}
}
\and
\inferrule*[lab={\ruleName{joint-sub-assoc}}]
{
  \Sigma \vdash \jointSub{\gty{\tau}}{\gty{\sigma}}{\tyUnion{\gty{\tau}'}{\gty{\sigma}'}}
}
{
  \Sigma \vdash \jointSub{\tyUnion{\gty{\tau}}{\gty{\tau}'}}{\gty{\sigma}}{\gty{\sigma}'}
}
\and
\inferrule*[lab={\ruleName{joint-sub-static}}]
{
  \Sigma \vdash \gty{\tau} \gsubty \sigma \\
  \Sigma \vdash \gty{\tau}' \gsubty \sigma
}
{
  \Sigma \vdash \jointSub{\gty{\tau}}{\sigma}{\gty{\tau}'}
}
\and
\inferrule*[lab={\ruleName{joint-sub-union-left}}]
{
  \Sigma \vdash \jointSub{\gty{\tau}}{\gty{\sigma}}{\gty{\tau}'}
}
{
  \Sigma \vdash \jointSub{\gty{\tau}}{\tyUnion{\gty{\sigma}}{\gty{\sigma}'}}{\gty{\tau}'}
}
\and
\inferrule*[lab={\ruleName{joint-sub-union-right}}]
{
  \Sigma \vdash \jointSub{\gty{\tau}}{\gty{\sigma}'}{\gty{\tau}'}
}
{
  \Sigma \vdash \jointSub{\gty{\tau}}{\tyUnion{\gty{\sigma}}{\gty{\sigma}'}}{\gty{\tau}'}
}
\and
\inferrule*[lab={\ruleName{joint-sub-split}}]
{
  \Sigma \vdash \gty{\tau} \gsubty \gty{\sigma} \\
  \Sigma \vdash \gty{\tau}' \gsubty \gty{\sigma}'
}
{
  \Sigma \vdash \jointSub{\gty{\tau}}{\tyUnion{\gty{\sigma}}{\gty{\sigma}'}}{\gty{\tau}'}
}
\and
\inferrule*[lab={\ruleName{joint-sub-list}}]
{
  \Sigma \vdash \jointEquiv{\gty{\tau}}{\gty{\sigma}}{\gty{\tau}'}
}
{
  \Sigma \vdash \jointSub{\tyList{\gty{\tau}}}{\tyList{\gty{\sigma}}}{\tyList{\gty{\tau}'}}
}
\and
\inferrule*[lab={\ruleName{joint-sub-dict}}]
{
  \Sigma \vdash \jointEquiv{\gty{\tau}}{\gty{\sigma}}{\gty{\tau}'}
}
{
  \Sigma \vdash \jointSub{\tyDict{\gty{\tau}}}{\tyDict{\gty{\sigma}}}{\tyDict{\gty{\tau}'}}
}
\and
\inferrule*[lab={\ruleName{joint-sub-tuple}}]
{
  \Sigma \vdash \jointSub{\gty{\tau}_i}{\gty{\sigma}_i}{\gty{\tau}'_i} \quad (\forall i)
}
{
  \Sigma \vdash \jointSub{\tyTuple{\gty{\tau}_1, \ldots, \gty{\tau}_n}}{\tyTuple{\gty{\sigma}_1, \ldots, \gty{\sigma}_n}}{\tyTuple{\gty{\tau}'_1, \ldots, \gty{\tau}'_n}}
}
\and
\inferrule*[lab={\ruleName{joint-sub-callable}}]
{
  \Sigma \vdash \jointSup{\gty{\tau}_i}{\gty{\sigma}_i}{\gty{\tau}'_i} \quad (\forall i) \\
  \Sigma \vdash \jointSub{\gty{\tau}}{\gty{\sigma}}{\gty{\tau}'}
}
{
  \Sigma \vdash \jointSub{\tyCallable{\gty{\tau}_1, \ldots, \gty{\tau}_n}{\gty{\tau}}}{\tyCallable{\gty{\sigma}_1, \ldots, \gty{\sigma}_n}{\gty{\sigma}}}{\tyCallable{\gty{\tau}'_1, \ldots, \gty{\tau}'_n}{\gty{\tau}'}}
}
\end{mathpar}
\caption{$\jointSub{\gty{\tau}}{\gty{\sigma}}{\gty{\tau}'}$; symmetric variants omitted}
\label{fig:graduality-joint-sub}
\end{figure}
