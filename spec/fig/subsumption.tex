\begin{figure}
\flushleft \shadebox{$\Gamma \mathrel{\vdash} p \subsumed p'$}
\begin{mathpar}
\small
\inferrule*[lab={\ruleName{sub-lit}}]
{
  \strut
}
{
  \Gamma \mathrel{\vdash} \ell \subsumed \ell
}
\and
\inferrule*[lab={\ruleName{sub-neg-lit}}]
{
  \strut
}
{
  \Gamma \mathrel{\vdash} -n \subsumed -n
}
\and
\inferrule*[lab={\ruleName{sub-var}}]
{
  \strut
}
{
  \Gamma \mathrel{\vdash} p \subsumed x
}
\and
\inferrule*[lab={\ruleName{sub-wild}}]
{
  \strut
}
{
  \Gamma \mathrel{\vdash} p \subsumed \patWild
}
\and
\inferrule*[lab={\ruleName{sub-list-list}}]
{
  \Gamma \mathrel{\vdash} p_i \subsumed q_i \quad (\forall i)
}
{
  \Gamma \mathrel{\vdash} \patList{p_1, \ldots, p_n} \subsumed \patList{q_1, \ldots, q_n}
}
\and
\inferrule*[lab={\ruleName{sub-tuple-tuple}}]
{
  \Gamma \mathrel{\vdash} p_i \subsumed q_i \quad (\forall i)
}
{
  \Gamma \mathrel{\vdash} \patTuple{p_1, \ldots, p_n} \subsumed \patTuple{q_1, \ldots, q_n}
}
\and
\inferrule*[lab={\ruleName{sub-dict-dict}}]
{
  \set{u_1, \ldots, u_m} \subseteq \set{w_1, \ldots, w_n} \\
  \Gamma \mathrel{\vdash} p_i \subsumed q_j \quad (\forall i, j.\ w_i = u_j)
}
{
  \Gamma \mathrel{\vdash} \patDict{\bind{w_1}{p_1}, \ldots, \bind{w_n}{p_n}} \subsumed \patDict{\bind{u_1}{q_1}, \ldots, \bind{u_m}{q_m}}
}
\and
\inferrule*[lab={\ruleName{sub-constr}}]
{
  \Gamma \vdash q : C \\
  \Gamma \vdash q' : C' \\
  C' \in \ancestors(C) \\
  \Gamma \vdash \fieldMap(C, \seq{p})(x) \subsumed \fieldMap(C', \seq{p}\,')(x) \quad (\forall x \in \fields(C'))
}
{
  \Gamma \vdash \patConstr{q}{\seq{p}} \subsumed \patConstr{q'}{\seq{p}\,'}
}
\and
\inferrule*[lab={\ruleName{sub-as-l}}]
{
  \Gamma \mathrel{\vdash} p \subsumed q
}
{
  \Gamma \mathrel{\vdash} p\;\pyinline{as}\;x \subsumed q
}
\and
\inferrule*[lab={\ruleName{sub-as-r}}]
{
  \Gamma \mathrel{\vdash} p \subsumed q
}
{
  \Gamma \mathrel{\vdash} p \subsumed q\;\pyinline{as}\;y
}
\end{mathpar}
\caption{Pattern $p$ is subsumed by pattern $p'$ under $\Gamma$}
\label{fig:subsumption}
\end{figure}
