\begin{figure}
\flushleft \shadebox{$\Gamma \vdash t : R$}
\begin{mathpar}
\small
\inferrule*[lab={\ruleName{pass}}]
{
  \strut
}
{
  \Gamma \vdash \exPass : \Assigns{\varnothing}
}
\and
\inferrule*[lab={\ruleName{assign}}]
{
  \Gamma \vdash e \\
  x \notin \capturesE(e)
}
{
  \Gamma \vdash \exAssign{x\pi}{e} : \Assigns{\set{x : \statusDefAssigned}}
}
\and
\inferrule*[lab={\ruleName{expr-stmt}}]
{
  \Gamma \vdash e
}
{
  \Gamma \vdash e : \Assigns{\varnothing}
}
\and
\inferrule*[lab={\ruleName{assert}}]
{
  \Gamma \vdash e
}
{
  \Gamma \vdash \exAssert{e} : \Assigns{\varnothing}
}
\and
\inferrule*[lab={\ruleName{assert-msg}}]
{
  \Gamma \vdash e \\
  \Gamma \vdash e'
}
{
  \Gamma \vdash \exAssertMsg{e}{e'} : \Assigns{\varnothing}
}
\and
\inferrule*[lab={\ruleName{return-none}}]
{
  \strut
}
{
  \Gamma \vdash \exReturnNone : \Returns
}
\and
\inferrule*[lab={\ruleName{return}}]
{
  \Gamma \vdash e
}
{
  \Gamma \vdash \exReturn{e} : \Returns
}
\and
\inferrule*[lab={\ruleName{if}}]
{
  \Gamma \vdash e_i \quad (\forall i) \\
  \Gamma \vdash s_i : R_i \quad (\forall i)
}
{
  \Gamma \vdash \exIf{e_1}{s_1}{\exElif{e_2}{s_2}\;\ldots\;\exElif{e_{n+1}}{s_{n+1}}} : R_1 \merge \ldots \mathbin{\merge} R_{n+1} \:  \merge \Assigns{\varnothing}
}
\and
\inferrule*[lab={\ruleName{if-else}}]
{
  \Gamma \vdash e_i \quad (\forall i) \\
  \Gamma \vdash s_i : R_i \quad (\forall i)
}
{
  \Gamma \vdash \exIfElse{e_1}{s_1}{\exElif{e_2}{s_2}\;\ldots\;\exElif{e_{n+1}}{s_{n+1}}}{s_{n+2}} : R_1 \merge \ldots \merge R_{n+2}
}
\and
\inferrule*[lab={\ruleName{def}}]
{
  \Gamma \override \statusDefAssigned(\seq{f} \mathbin{\concat} \seq{x}_i) \override \statusNotDefAssigned(\assignsS(s_i) \setminus \seq{x}_i) \vdash s_i : R_i \quad (\forall i)
  \\
  \seq{f} \text{ distinct}
}
{
  \Gamma \vdash \exDef{f_1}{\seq{x}_1\,\seq{\pi}_1}{\omega_1}{s_1} \cons \ldots \cons \exDef{f_{n+1}}{\seq{x}_{n+1}\,\seq{\pi}_{n+1}}{\omega_{n+1}}{s_{n+1}} : \Assigns{\statusDefAssigned(\seq{f})}
}
\and
\input{spec/rules/match}
\and
\inferrule*[lab={\ruleName{match-partial}}]
{
  \Gamma \vdash e \\
  \Gamma \override \statusDefAssigned(\binds(p_i)) \vdash s_i : R_i \quad (\forall i) \\
  S_i = \Assigns{\statusDefAssigned(\binds(p_i))} \override R_i \quad (\forall i) \\
  \Gamma \vdash \seq{p} \\
  p_{n+1} \notin \set{x, \patWild}
}
{
  \Gamma \vdash \exMatch{e}{\exCase{p_1}{s_1}\;\ldots\;\exCase{p_{n+1}}{s_{n+1}}} : S_1 \merge \ldots \merge S_{n+1} \merge \Assigns{\varnothing}
}
\and
\inferrule*[lab={\ruleName{seq}}]
{
  \Gamma \vdash t : \Assigns{\Delta} \\
  \Gamma \override \Delta \vdash t' : R \\
  \capturesS(t) \cap \assignsS(t') = \varnothing
}
{
  \Gamma \vdash t\;t' : \Assigns{\Delta} \override R
}
\and
\inferrule*[lab={\ruleName{class}}]
{
  \nexists C'.\ \bind{c}{C'} \in \Gamma \\
  \ownFields(\phi) = \bind{\seq{x}}{\seq{\psi}}, \; \seq{x} \text{ distinct} \\
  C = \clsEntry{\Gamma}{q.c}{\bind{\seq{x}}{\seq{\tau}}}{\bot}
}
{
  \Gamma \vdash \exDataclass{c}{\phi} : \Assigns{\set{\bind{c}{C}}}
}
\and
\inferrule*[lab={\ruleName{class-extend}}]
{
  \nexists C'.\ \bind{c}{C'} \in \Gamma \\
  \bind{c'}{\clsEntry{\Gamma'}{q.c'}{\seq{y}}{c^\dagger}} \in \Gamma \\
  \ownFields(\phi) = \bind{\seq{x}}{\seq{\psi}}, \; \seq{x} \text{ distinct} \\
  \seq{x} \cap \dom(\fields(\Gamma(c'))) = \varnothing \\
  C = \clsEntry{\Gamma}{q.c}{\bind{\seq{x}}{\seq{\tau}}}{c'}
}
{
  \Gamma \vdash \exDataclassExtend{c}{c'}{\phi} : \Assigns{\set{\bind{c}{C}}}
}
\end{mathpar}
\caption{Statement $t$ is well-formed in $\Gamma$ with result type $R$}
\label{fig:well-formed}
\end{figure}
