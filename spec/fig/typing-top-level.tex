\begin{figure}
\flushleft \shadebox{$\Sigma; q; \Gamma \vdash t : R \binding \Sigma'$}
\begin{mathpar}
\small
\inferrule*[lab={\ruleName{top-stmt}}]
{
  \Sigma; \Gamma; \varepsilon \vdash s : R
}
{
  \Sigma; q; \Gamma \vdash s : R \binding \Sigma
}
\and
\inferrule*[lab={\ruleName{top-seq}}]
{
  \Sigma; q; \Gamma \vdash t : \Assigns{\Delta} \binding \Sigma^\dagger \\
  \Sigma^\dagger; q; \Gamma \override \Delta \vdash t' : R \binding \Sigma' \\
}
{
  \Sigma; q; \Gamma \vdash t\;t' : \Assigns{\Delta} \override R \binding \Sigma'
}
\and
\inferrule*[lab={\ruleName{class}}]
{
  \bind{\pyinline{dataclass}}{\predefName} \in \Gamma \\
  \Gamma' = \Gamma \override \set{\bind{\alpha_1}{\TypeVar}, \ldots, \bind{\alpha_n}{\TypeVar}} \\
  \ownFields(\phi) = \bind{\seq{x}}{\seq{\psi}}, \; \seq{x} \text{ distinct} \\
  \replaced{\Sigma; \Gamma'}{\Gamma'} \vdash \psi_i \synth \sigma_i \quad (\forall i)
}
{
  \Sigma; q; \Gamma \vdash \exDataclass{c[\seq{\alpha}]}{\phi} : \Assigns{\set{\bind{c}{\Type{\fq{q.c}}}}} \binding \Sigma, \bind{\fq{q.c}}{\clsRec{\seq{\alpha}}{\bind{\seq{x}}{\seq{\sigma}}}{\bot}}
}
\and
\inferrule*[lab={\ruleName{class-extend}}]
{
  \Gamma' = \Gamma \override \set{\bind{\alpha_1}{\TypeVar}, \ldots, \bind{\alpha_n}{\TypeVar}} \\
  \replaced{\Sigma; \Gamma' \vdash q'[\seq{\psi}'] \synth D[\seq{\sigma}']}{\Gamma' \vdash q' : \Type{D}} \\
  \deleted{\Gamma' \vdash \psi'_i \synth \sigma'_i \quad (\forall i), \; D \text{ has } |\seq{\sigma}'| \text{ type parameters in } \Sigma} \\
  \bind{\pyinline{dataclass}}{\predefName} \in \Gamma \\
  \ownFields(\phi) = \bind{\seq{x}}{\seq{\psi}}, \; \seq{x} \text{ distinct} \\
  \replaced{\Sigma; \Gamma'}{\Gamma'} \vdash \psi_i \synth \sigma_i \quad (\forall i) \\
  \fields(\Sigma, D) = \scheme{\seq{\alpha}'}{\bind{\seq{y}}{\seq{\tau}}} \\
  \set{\seq{x}} \cap \set{\seq{y}} = \varnothing
}
{
  \Sigma; q; \Gamma \vdash \exDataclassExtend{c[\seq{\alpha}]}{q'[\seq{\psi}']}{\phi} : \Assigns{\set{\bind{c}{\Type{\fq{q.c}}}}} \binding \Sigma, \bind{\fq{q.c}}{\clsRec{\seq{\alpha}}{\bind{\seq{x}}{\seq{\sigma}}}{D[\seq{\sigma}']}}
}
\end{mathpar}
\caption{Top-level statement $t$ of module $q$ has static outcome $R$, extending $\Sigma$ to $\Sigma'$}
\label{fig:typing-top-level}
\end{figure}
