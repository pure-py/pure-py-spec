\begin{figure}
\small
\setlength{\jot}{2pt}
\flushleft \shadebox{$\Sigma; \Gamma \vdash \seq{p} : \sigma \matches \seq{\Delta}, T$}
\begin{mathpar}
\small
\inferrule*[lab={\ruleName{desc-cases}}]
{
  T_0 = \top \\
  \Sigma; \Gamma \vdash p_i \agrees \sigma \quad (\forall i) \\
  \Sigma; \Gamma \vdash T_{i-1} : \sigma, p_i \matches S_i, T_i \binding \Delta_i \quad (\forall i) \\
  \Sigma \vdash S_i : \sigma \synth \tau_i \\
  \tau_i \neq \tyNever \quad (\forall i)
}
{
  \Sigma; \Gamma \vdash \seq{p} : \sigma \matches \seq{\Delta}, T_{n+1}
}
\end{mathpar}

\vspace{2mm}

\flushleft \shadebox{$\Sigma; \Gamma \vdash T : \seq{\tau}, p \matches S, T' \binding \Delta$}
\begin{mathpar}
\small
\inferrule*[lab={\ruleName{desc-wild}}]
{
  \strut
}
{
  \Sigma; \Gamma \vdash T : \seq{\tau}, \patWild \matches T, \bot \binding \varnothing
}
\and
\inferrule*[lab={\ruleName{desc-var}}]
{
  \Sigma \vdash T : \seq{\tau} \synth \sigma
}
{
  \Sigma; \Gamma \vdash T : \seq{\tau}, x \matches T, \bot \binding \set{\bind{x}{\sigma}}
}
\and
\inferrule*[lab={\ruleName{desc-lit}}]
{
  \strut
}
{
  \Sigma; \Gamma \vdash \descHeads{\set{\bind{\ell}{T}, \bind{\seq{h}}{\seq{T}}}}{U} : \seq{\tau}, \ell
    \matches \descHeads{\set{\bind{\ell}{T}}}{\bot},
    \descHeads{\set{\bind{\ell}{\bot}, \bind{\seq{h}}{\seq{T}}}}{U} \binding \varnothing
}
\and
\inferrule*[lab={\ruleName{desc-lit-default}}]
{
  \ell \notin \seq{h} \\
  \Sigma \vdash \tyLiteral{\ell} \subty \tau
}
{
  \Sigma; \Gamma \vdash \descHeads{\set{\bind{\seq{h}}{\seq{T}}}}{U} : \tau \cons \seq{\tau}, \ell
    \matches \descHeads{\set{\bind{\ell}{U}}}{\bot},
    \descHeads{\set{\bind{\ell}{\bot}, \bind{\seq{h}}{\seq{T}}}}{U} \binding \varnothing
}
\and
\inferrule*[lab={\ruleName{desc-neg-lit}}]
{
  \strut
}
{
  \Sigma; \Gamma \vdash \descHeads{\set{\bind{-n}{T}, \bind{\seq{h}}{\seq{T}}}}{U} : \seq{\tau}, -n
    \matches \descHeads{\set{\bind{-n}{T}}}{\bot},
    \descHeads{\set{\bind{-n}{\bot}, \bind{\seq{h}}{\seq{T}}}}{U} \binding \varnothing
}
\and
\inferrule*[lab={\ruleName{desc-neg-lit-default}}]
{
  -n \notin \seq{h} \\
  \Sigma \vdash \tyLiteral{-n} \subty \tau
}
{
  \Sigma; \Gamma \vdash \descHeads{\set{\bind{\seq{h}}{\seq{T}}}}{U} : \tau \cons \seq{\tau}, -n
    \matches \descHeads{\set{\bind{-n}{U}}}{\bot},
    \descHeads{\set{\bind{-n}{\bot}, \bind{\seq{h}}{\seq{T}}}}{U} \binding \varnothing
}
\and
\inferrule*[lab={\ruleName{desc-as}}]
{
  \Sigma; \Gamma \vdash T : \seq{\tau}, p \matches S, T' \binding \Delta \\
  \Sigma \vdash S : \seq{\tau} \synth \sigma
}
{
  \Sigma; \Gamma \vdash T : \seq{\tau}, p\;\pyinline{as}\;x \matches S, T' \binding \Delta \disjunion \set{\bind{x}{\sigma}}
}
\and
\inferrule*[lab={\ruleName{desc-union}}]
{
  \Sigma; \Gamma \vdash T : \tau \cons \seq{\tau}, p \matches S_1, T_1 \binding \Delta_1 \\
  \Sigma; \Gamma \vdash T' : \sigma \cons \seq{\tau}, p \matches S_2, T_2 \binding \Delta_2 \\
  \Delta = \set{\bind{x}{\joinTy(\Sigma, \Delta_1(x), \Delta_2(x))} \mid x \in \binds(p)}
}
{
  \Sigma; \Gamma \vdash \tyUnion{T}{T'} : (\tyUnion{\tau}{\sigma}) \cons \seq{\tau}, p
    \matches \tyUnion{S_1}{S_2}, \tyUnion{T_1}{T_2} \binding \Delta
}
\and
\inferrule*[lab={\ruleName{desc-top-heads}}]
{
  \Sigma; \Gamma \vdash \descHeads{\varnothing}{\top} : \tau \cons \seq{\tau}, p \matches S, T' \binding \Delta \\
  \tau \text{ neither union, tuple nor dictionary type}
}
{
  \Sigma; \Gamma \vdash \top : \tau \cons \seq{\tau}, p \matches S, T' \binding \Delta
}
\and
\inferrule*[lab={\ruleName{desc-bot-heads}}]
{
  \Sigma; \Gamma \vdash \descHeads{\varnothing}{\bot} : \tau \cons \seq{\tau}, p \matches S, T' \binding \Delta \\
  \tau \text{ neither union, tuple nor dictionary type}
}
{
  \Sigma; \Gamma \vdash \bot : \tau \cons \seq{\tau}, p \matches S, T' \binding \Delta
}
\and
\inferrule*[lab={\ruleName{desc-top-union}}]
{
  \Sigma; \Gamma \vdash \tyUnion{\top}{\top} : (\tyUnion{\tau}{\sigma}) \cons \seq{\tau}, p \matches S, T' \binding \Delta
}
{
  \Sigma; \Gamma \vdash \top : (\tyUnion{\tau}{\sigma}) \cons \seq{\tau}, p \matches S, T' \binding \Delta
}
\and
\inferrule*[lab={\ruleName{desc-bot-union}}]
{
  \Sigma; \Gamma \vdash \tyUnion{\bot}{\bot} : (\tyUnion{\tau}{\sigma}) \cons \seq{\tau}, p \matches S, T' \binding \Delta
}
{
  \Sigma; \Gamma \vdash \bot : (\tyUnion{\tau}{\sigma}) \cons \seq{\tau}, p \matches S, T' \binding \Delta
}
\end{mathpar}
\caption{Pattern matching over value descriptors}
\label{fig:pat-descriptors}
\end{figure}
