\begin{figure}
\small
\setlength{\jot}{2pt}
\flushleft \shadebox{$\Sigma; \Gamma \vdash \seq{p} : \sigma \matches \seq{\Delta}, T$}
\begin{mathpar}
\small
\inferrule*[lab={\ruleName{desc-cases}}]
{
  T_0 = \top \\
  \Sigma; \Gamma \vdash p_i \agrees \sigma \quad (\forall i) \\
  \Sigma; \Gamma \vdash T_{i-1} : \sigma, p_i \matches S_i, T_i \binding \Delta_i \quad (\forall i) \\
  \Sigma \vdash S_i : \sigma \synth \tau_i \\
  \tau_i \neq \tyNever \quad (\forall i)
}
{
  \Sigma; \Gamma \vdash \seq{p} : \sigma \matches \seq{\Delta}, T_{n+1}
}
\end{mathpar}

\vspace{2mm}

\flushleft \shadebox{$\Sigma; \Gamma \vdash T : \seq{\tau}, \seq{p} \matches S, T' \binding \Delta$}
\begin{mathpar}
\small
\inferrule*[lab={\ruleName{desc-empty}}]
{
  \strut
}
{
  \Sigma; \Gamma \vdash T : \varepsilon, \varepsilon \matches T, \bot \binding \varnothing
}
\and
\inferrule*[lab={\ruleName{desc-wild}}]
{
  p_i = \patWild \quad (\forall i)
}
{
  \Sigma; \Gamma \vdash T : \seq{\tau}, \seq{p} \matches T, \bot \binding \varnothing
}
\and
\inferrule*[lab={\ruleName{desc-wild-default}}]
{
  \Sigma; \Gamma \vdash U : \seq{\tau}, \seq{p} \matches S, T' \binding \Delta
}
{
  \Sigma; \Gamma \vdash \descHeads{\varnothing}{U} : \tau \cons \seq{\tau}, \patWild \cons \seq{p}
    \matches \descHeads{\varnothing}{S}, \descHeads{\varnothing}{T'} \binding \Delta
}
\and
\inferrule*[lab={\ruleName{desc-var}}]
{
  \Sigma; \Gamma \vdash T : \seq{\tau}, \patWild \cons \seq{p} \matches S, T' \binding \Delta \\
  \Sigma \vdash S : \seq{\tau} \synth \sigma
}
{
  \Sigma; \Gamma \vdash T : \seq{\tau}, x \cons \seq{p} \matches S, T' \binding \Delta \disjunion \set{\bind{x}{\sigma}}
}
\and
\inferrule*[lab={\ruleName{desc-as}}]
{
  \Sigma; \Gamma \vdash T : \seq{\tau}, p \cons \seq{p} \matches S, T' \binding \Delta \\
  \Sigma \vdash S : \seq{\tau} \synth \sigma
}
{
  \Sigma; \Gamma \vdash T : \seq{\tau}, (p\;\pyinline{as}\;x) \cons \seq{p} \matches S, T' \binding \Delta \disjunion \set{\bind{x}{\sigma}}
}
\and
\inferrule*[lab={\ruleName{desc-tuple}}]
{
  \Sigma; \Gamma \vdash T : \seq{\sigma} \concat \seq{\tau}, \seq{p}\,' \concat \seq{p} \matches S, T' \binding \Delta
}
{
  \Sigma; \Gamma \vdash T : \tyTuple{\seq{\sigma}} \cons \seq{\tau}, \patTuple{\seq{p}\,'} \cons \seq{p} \matches S, T' \binding \Delta
}
\and
\inferrule*[lab={\ruleName{desc-union}}]
{
  \Sigma; \Gamma \vdash T : \tau \cons \seq{\tau}, \seq{p} \matches S_1, T_1 \binding \Delta_1 \\
  \Sigma; \Gamma \vdash T' : \sigma \cons \seq{\tau}, \seq{p} \matches S_2, T_2 \binding \Delta_2 \\
  \Delta = \set{\bind{x}{\joinTy(\Sigma, \Delta_1(x), \Delta_2(x))} \mid x \in \binds(\seq{p})}
}
{
  \Sigma; \Gamma \vdash \tyUnion{T}{T'} : (\tyUnion{\tau}{\sigma}) \cons \seq{\tau}, \seq{p}
    \matches \tyUnion{S_1}{S_2}, \tyUnion{T_1}{T_2} \binding \Delta
}
\and
\inferrule*[lab={\ruleName{desc-top-heads}}]
{
  \Sigma; \Gamma \vdash \descHeads{\varnothing}{\top} : \tau \cons \seq{\tau}, \seq{p} \matches S, T' \binding \Delta \\
  \tau \text{ neither union, tuple nor dictionary type}
}
{
  \Sigma; \Gamma \vdash \top : \tau \cons \seq{\tau}, \seq{p} \matches S, T' \binding \Delta
}
\and
\inferrule*[lab={\ruleName{desc-bot-heads}}]
{
  \Sigma; \Gamma \vdash \descHeads{\varnothing}{\bot} : \tau \cons \seq{\tau}, \seq{p} \matches S, T' \binding \Delta \\
  \tau \text{ neither union, tuple nor dictionary type}
}
{
  \Sigma; \Gamma \vdash \bot : \tau \cons \seq{\tau}, \seq{p} \matches S, T' \binding \Delta
}
\and
\inferrule*[lab={\ruleName{desc-top-union}}]
{
  \Sigma; \Gamma \vdash \tyUnion{\top}{\top} : (\tyUnion{\tau}{\sigma}) \cons \seq{\tau}, \seq{p} \matches S, T' \binding \Delta
}
{
  \Sigma; \Gamma \vdash \top : (\tyUnion{\tau}{\sigma}) \cons \seq{\tau}, \seq{p} \matches S, T' \binding \Delta
}
\and
\inferrule*[lab={\ruleName{desc-bot-union}}]
{
  \Sigma; \Gamma \vdash \tyUnion{\bot}{\bot} : (\tyUnion{\tau}{\sigma}) \cons \seq{\tau}, \seq{p} \matches S, T' \binding \Delta
}
{
  \Sigma; \Gamma \vdash \bot : (\tyUnion{\tau}{\sigma}) \cons \seq{\tau}, \seq{p} \matches S, T' \binding \Delta
}
\end{mathpar}
\caption{Pattern matching over value descriptors}
\label{fig:pat-descriptors}
\end{figure}
