\begin{figure}
\small
\setlength{\jot}{2pt}
\flushleft \shadebox{$\Sigma; \Gamma \vdash T : \seq{\tau}, \seq{p} \matches S, T' \binding \Delta$ (continued)}
\begin{mathpar}
\small
\inferrule*[lab={\ruleName{desc-lit}}]
{
  E(\ell) = T \\
  \Sigma; \Gamma \vdash T : \seq{\tau}, \seq{p} \matches S, T' \binding \Delta
}
{
  \Sigma; \Gamma \vdash \descHeads{E}{U} : \tau \cons \seq{\tau}, \ell \cons \seq{p}
    \matches \descHeads{\set{\bind{\ell}{S}}}{\bot},
    \descHeads{E \override \set{\bind{\ell}{T'}}}{U} \binding \Delta
}
\and
\inferrule*[lab={\ruleName{desc-lit-default}}]
{
  \ell \notin \dom(E) \\
  \Sigma \vdash \tyLiteral{\ell} \subty \tau \\
  \Sigma; \Gamma \vdash U : \seq{\tau}, \seq{p} \matches S, T' \binding \Delta
}
{
  \Sigma; \Gamma \vdash \descHeads{E}{U} : \tau \cons \seq{\tau}, \ell \cons \seq{p}
    \matches \descHeads{\set{\bind{\ell}{S}}}{\bot},
    \descHeads{E \override \set{\bind{\ell}{T'}}}{U} \binding \Delta
}
\and
\inferrule*[lab={\ruleName{desc-neg-lit}}]
{
  E(-n) = T \\
  \Sigma; \Gamma \vdash T : \seq{\tau}, \seq{p} \matches S, T' \binding \Delta
}
{
  \Sigma; \Gamma \vdash \descHeads{E}{U} : \tau \cons \seq{\tau}, -n \cons \seq{p}
    \matches \descHeads{\set{\bind{-n}{S}}}{\bot},
    \descHeads{E \override \set{\bind{-n}{T'}}}{U} \binding \Delta
}
\and
\inferrule*[lab={\ruleName{desc-neg-lit-default}}]
{
  -n \notin \dom(E) \\
  \Sigma \vdash \tyLiteral{-n} \subty \tau \\
  \Sigma; \Gamma \vdash U : \seq{\tau}, \seq{p} \matches S, T' \binding \Delta
}
{
  \Sigma; \Gamma \vdash \descHeads{E}{U} : \tau \cons \seq{\tau}, -n \cons \seq{p}
    \matches \descHeads{\set{\bind{-n}{S}}}{\bot},
    \descHeads{E \override \set{\bind{-n}{T'}}}{U} \binding \Delta
}
\and
\inferrule*[lab={\ruleName{desc-list}}]
{
  E(\headList{n}) = T \\
  \sigma_i = \tau \quad (\forall i \leq n) \\
  \Sigma; \Gamma \vdash T : \sigma_1, \ldots, \sigma_n \concat \seq{\tau}, \seq{p}\,' \concat \seq{p} \matches S, T' \binding \Delta
}
{
  \Sigma; \Gamma \vdash \descHeads{E}{U} : \tyList{\tau} \cons \seq{\tau}, \patList{\seq{p}\,'} \cons \seq{p}
    \matches \descHeads{\set{\bind{\headList{n}}{S}}}{\bot},
    \descHeads{E \override \set{\bind{\headList{n}}{T'}}}{U} \binding \Delta
}
\end{mathpar}
\caption{Pattern matching at named heads}
\label{fig:pat-descriptors-heads}
\end{figure}
