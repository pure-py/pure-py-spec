% Index of judgement forms (specification only).
\begin{center}
\small
\begin{tabular}{>{\raggedright\arraybackslash}p{0.22\textwidth}>{\raggedright\arraybackslash}p{0.55\textwidth}>{\raggedright\arraybackslash}p{0.13\textwidth}}
\textbf{Judgement} & \textbf{Reading} & \textbf{Defined in} \\[1mm]
\metaGroup{Well-formedness} \\
$\Sigma \vdash h : \tau$ & head $h$ has type $\tau$ & \figref{shapes} \\
$\Sigma \vdash k : \tau$ & shape $k$ has type $\tau$ & \figref{shapes} \\
$\Sigma; \Gamma \vdash p \agrees \tau$ & tuple and list patterns in $p$ meet only their own kind at $\tau$ & \figref{agrees} \\
$\Sigma; \Gamma \vdash k, p \splits K, L$ & shape $k$ splits at the head that $p$ tests for, giving $K$ and $L$ & \figref{split} \\
$\Sigma; \Gamma \vdash k, p \matches K, L \binding \Delta$ & pattern $p$ matches shapes $K$ of $k$, leaving $L$ and binding $\Delta$ & \figref{pat} \\
$\Sigma; \Gamma; \sigma \vdash \seq{p} \matches \seq{\Delta}, L$ & case patterns $\seq{p}$ over $\sigma$ bind $\seq{\Delta}$, leaving $L$ & \figref{pat-seq} \\
$\Gamma \vdash q : \theta$ & qualified name $q$ denotes entry $\theta$ & \figref{resolve-name} \\
$\Sigma; \Gamma \vdash e \synth \tau$ & expression $e$ synthesises type $\tau$ & \figref{typing-synth} \\
$\Sigma; \Gamma \vdash e \checks \tau$ & expression $e$ checks against type $\tau$ & \figref{typing-check} \\
$\Sigma; \Gamma \vdash \seq{g} : \Delta$ & comprehension qualifiers $\seq{g}$ bind $\Delta$ & \figref{typing-check} \\
$\Sigma; \Gamma; \zeta \vdash s : R$ & statement $s$ has result type $R$, with return type $\zeta$ & \figref{typing-stmt} \\
$\Sigma; q; \Gamma \vdash t : R \binding \Sigma'$ & top-level statement $t$ has result type $R$, extending $\Sigma$ to $\Sigma'$ & \figref{typing-top} \\
$\Sigma \vdash \Gamma, x \importsR \theta \binding \Sigma'$ & name $x$ imported from module signature $\Gamma$ has entry $\theta$ & \figref{typing-imports} \\
$\Sigma; q \vdash \seq{\iota} : \Gamma \binding \Sigma'$ & imports $\seq{\iota}$ of module $q$ yield context $\Gamma$ & \figref{typing-import-prefix} \\
$\Sigma \vdash q, \theta \loadsAs \theta' \binding \Sigma'$ & importing module $q$ into entry $\theta$ gives entry $\theta'$ & \figref{typing-import-prefix} \\
$\Sigma \vdash_{\mathsf{load}} q : \Gamma \binding \Sigma'$ & module $q$ statically loads with signature $\Gamma$ & \figref{typing-module} \\
$\vdash \mathcal{M}$ & program $\mathcal{M}$ is well-formed & \figref{typing-module}
   \\[2mm]
\metaGroup{Evaluation} \\
$\vdash v : \theta$ & value $v$ has entry $\theta$ & \figref{value-typing} \\
$\vdash \rho : \Gamma$ & environment $\rho$ has context $\Gamma$ & \figref{value-typing} \\
$\rho, p, v \matches m$ & pattern $p$ matches value $v$ with result $m$ & \figref{eval-pat} \\
$\rho, t \Rightarrow r$ & statement $t$ evaluates to result $r$ & \figref{eval-block-outcome} \\
$\rho, e \Rightarrow o$ & expression $e$ evaluates to outcome $o$ & \figref{eval-outcome} \\
$\rho, \seq{e} \Rightarrow o$ & expressions $\seq{e}$ evaluate, left to right, to outcome $o$ & \figref{eval-seq} \\
$\rho, \seq{g} \Rightarrow o$ & comprehension qualifiers $\seq{g}$ evaluate to outcome $o$ & \figref{eval-quals-outcome} \\
$\rho, x \importsR o$ & name $x$ imported from module environment $\rho$ has outcome $o$ & \figref{eval-imports} \\
$\seq{\iota} \Rightarrow o$ & imports $\seq{\iota}$ evaluate to outcome $o$ & \figref{eval-import-prefix} \\
$q, v \loadsAs v'$ & importing module $q$ into value $v$ gives value $v'$ & \figref{eval-import-prefix} \\
$q \Rightarrow_{\mathsf{load}} o$ & module $q$ loads to outcome $o$ & \figref{eval-modules} \\
$\mathcal{M} \Rightarrow \kappa$ & program $\mathcal{M}$ evaluates to termination kind $\kappa$ & \figref{eval-modules}
\end{tabular}
\end{center}
