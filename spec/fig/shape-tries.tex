\begin{figure}
\small
\setlength{\jot}{2pt}
\begin{center}
\begin{tabular}{rcll}
\grammarGroup{Heads} \\
$h$ & $::=$ & $\ell$ & (Literal) \\
    & $\mid$ & $C$ & (Class)
   \\[3mm]
\grammarGroup{Tries} \\
$t$ & $::=$ & $\top$ & (All) \\
    & $\mid$ & $\bot$ & (None) \\
    & $\mid$ & $\tyUnion{t}{t'}$ & (Union) \\
    & $\mid$ & $\trieSwitch{\bind{\seq{h}}{\seq{t}}}{d}$ & (Switch) \\
    & $\mid$ & $\trieDict{\bind{\seq{w}}{\seq{t}}}{d}$ & (Dict)
   \\[3mm]
\grammarGroup{Defaults} \\
$d$ & $::=$ & $\top$ & (All) \\
    & $\mid$ & $\bot$ & (None)
\end{tabular}
\end{center}

\vspace{2mm}

\flushleft \shadebox{$\Sigma \vdash t : \seq{\tau}$}
\begin{mathpar}
\small
\inferrule*[lab={\ruleName{trie-top}}]
{
  \strut
}
{
  \Sigma \vdash \top : \seq{\tau}
}
\and
\inferrule*[lab={\ruleName{trie-bot}}]
{
  \strut
}
{
  \Sigma \vdash \bot : \seq{\tau}
}
\and
\inferrule*[lab={\ruleName{trie-union}}]
{
  \Sigma \vdash t : \tau \cons \seq{\tau} \\
  \Sigma \vdash t' : \sigma \cons \seq{\tau}
}
{
  \Sigma \vdash \tyUnion{t}{t'} : (\tyUnion{\tau}{\sigma}) \cons \seq{\tau}
}
\and
\inferrule*[lab={\ruleName{trie-tuple}}]
{
  \Sigma \vdash t : \seq{\sigma} \concat \seq{\tau}
}
{
  \Sigma \vdash t : \tyTuple{\seq{\sigma}} \cons \seq{\tau}
}
\and
\inferrule*[lab={\ruleName{trie-switch}}]
{
  \Sigma \vdash \bind{\seq{h}}{\seq{t}} : \tau \cons \seq{\tau} \\
  \seq{h} \text{ distinct} \\
  \tau \text{ neither union, tuple nor dictionary type}
}
{
  \Sigma \vdash \trieSwitch{\bind{\seq{h}}{\seq{t}}}{d} : \tau \cons \seq{\tau}
}
\and
\inferrule*[lab={\ruleName{trie-dict}}]
{
  \Sigma \vdash t_i : \tau \cons \seq{\tau} \quad (\forall i) \\
  \seq{w} \text{ distinct}
}
{
  \Sigma \vdash \trieDict{\bind{\seq{w}}{\seq{t}}}{d} : \tyDict{\tau} \cons \seq{\tau}
}
\end{mathpar}

\vspace{2mm}

\flushleft \shadebox{$\Sigma \vdash \bind{\seq{h}}{\seq{t}} : \seq{\tau}$}
\begin{mathpar}
\small
\inferrule*[lab={\ruleName{entries-empty}}]
{
  \strut
}
{
  \Sigma \vdash \varepsilon : \tau \cons \seq{\tau}
}
\and
\inferrule*[lab={\ruleName{entries-literal}}]
{
  \Sigma \vdash \tyLiteral{\ell} \subty \tau \\
  \Sigma \vdash t : \seq{\tau} \\
  \Sigma \vdash \bind{\seq{h}}{\seq{t}} : \tau \cons \seq{\tau}
}
{
  \Sigma \vdash \bind{\ell}{t}, \bind{\seq{h}}{\seq{t}} : \tau \cons \seq{\tau}
}
\and
\inferrule*[lab={\ruleName{entries-class}}]
{
  \Sigma \vdash C \subty \tau \\
  \fields(\Sigma, C) = \seq{\bind{x}{\sigma}} \\
  \Sigma \vdash t : \seq{\sigma} \concat \seq{\tau} \\
  \Sigma \vdash \bind{\seq{h}}{\seq{t}} : \tau \cons \seq{\tau}
}
{
  \Sigma \vdash \bind{C}{t}, \bind{\seq{h}}{\seq{t}} : \tau \cons \seq{\tau}
}
\and
\inferrule*[lab={\ruleName{entries-length}}]
{
  \sigma_i = \tau \quad (\forall i \leq n) \\
  \Sigma \vdash t : \sigma_1, \ldots, \sigma_n \concat \seq{\tau} \\
  \Sigma \vdash \bind{\seq{h}}{\seq{t}} : \tyList{\tau} \cons \seq{\tau}
}
{
  \Sigma \vdash \bind{n}{t}, \bind{\seq{h}}{\seq{t}} : \tyList{\tau} \cons \seq{\tau}
}
\end{mathpar}

\caption{Shape tries}
\label{fig:shape-tries}
\end{figure}
