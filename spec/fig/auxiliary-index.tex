% Auxiliary functions with their meanings; the specification adds the section defining each.
\begin{center}
\small
\ifdefined\specmode
\begin{tabular}{>{\raggedright\arraybackslash}p{0.19\textwidth}>{\raggedright\arraybackslash}p{0.19\textwidth}>{\raggedright\arraybackslash}p{0.43\textwidth}>{\raggedright\arraybackslash}p{0.08\textwidth}}
\else
\begin{tabular}{>{\raggedright\arraybackslash}p{0.22\textwidth}>{\raggedright\arraybackslash}p{0.2\textwidth}>{\raggedright\arraybackslash}p{0.5\textwidth}}
\fi
\textbf{Function} & \textbf{Result} & \textbf{Meaning}\speconly{ & \textbf{Section}} \\[1mm]
\auxgroup{Syntactic analyses}
\auxrow{$\assignsS(s)$}{set of variables}{variables assigned in $s$, not descending into nested functions}{aux:assigns}
\auxrow{$\binds(p)$}{set of variables}{variables bound by pattern $p$}{aux:binds}
\auxrow{$\capturesS(s)$}{set of variables}{variables of enclosing scope captured by closures in $s$}{aux:captures}
\auxrow{$\capturesE(e)$}{set of variables}{variables of enclosing scope captured by closures in $e$}{aux:captures-e}
\auxrow{$\fvS(b)$}{set of variables}{free variables of block $b$}{aux:fv}
\auxrow{$\fvE(e)$}{set of variables}{free variables of expression $e$}{aux:fv-e}
\noalign{\vskip 2mm}
\auxgroup{Classes and modules}
\auxrow{$\submods(q)$}{context}{stub for each submodule of $q$}{aux:submods}
\auxrow{$\ownFields(\phi)$}{sequence of variables}{field names declared by field list $\phi$}{aux:own-fields}
\auxrow{$\fields(C)$}{sequence of variables}{fields of class $C$, inherited first}{aux:fields}
\auxrow{$\ancestors(C)$}{set of class entries}{$C$ and its base classes}{aux:ancestors}
\auxrow{$\fieldMap(C, \seq{u})$}{map from variables}{argument supplied for each field of $C$; undefined unless arguments cover fields exactly}{aux:field-map}
\auxrow{$\resolveClass(\rho, q)$}{class entry}{class denoted by qualified name $q$ in $\rho$}{aux:resolve-class}
\auxrow{$\env(\rho, q)$}{environment}{environment of module $q$ as loaded in $\rho$}{aux:env}
\auxrow{$\nameOf(q)$}{string}{string literal naming module $q$}{aux:name}
\noalign{\vskip 2mm}
\auxgroup{Values}
\auxrow{$\hat{\odot}(v, v')$}{outcome}{meaning of binary operator}{aux:binop}
\auxrow{$\hat{\ominus}(v)$}{outcome}{meaning of unary operator}{aux:unop}
\auxrow{$\eqOp(v, v')$}{Boolean}{structural equality; undefined between unrelated values}{aux:eq}
\specauxrow{$\eqElems(\seq{v}, \seq{v}\,')$}{Boolean}{elementwise equality of value sequences}{aux:eq-elems}
\auxrow{$\memOp(v, v')$}{Boolean}{whether $v'$ is an element of $v$}{aux:contains}
\specauxrow{$\memElems(\seq{v}, v)$}{Boolean}{whether $\seq{v}$ has an element equal to $v$}{aux:contains-elems}
\specauxrow{$\elems(v)$}{sequence of values}{elements of list, tuple or string}{aux:elems}
\auxrow{$\iterOp(v)$}{sequence of values}{sequence a generator draws from $v$; keys if $v$ is a dictionary}{aux:iter}
\auxrow{$\getitem(v, v')$}{outcome}{subscripting}{aux:getitem}
\auxrow{$\outcome(r)$}{outcome}{block result read as outcome of calling it}{aux:outcome}
\auxrow{$\dispatch(\rho, v, \seq{p}, \seq{b})$}{bindings and block}{case taken by a \pyinline{match}}{aux:dispatch}
\auxrow{$\entries(\delta, \delta')$}{dictionary entries}{$\delta$ extended by the entries of $\delta'$ in order, later overriding earlier}{aux:entries}
\auxrow{$\update(\delta, w, v)$}{dictionary entries}{$\delta$ with $w$ bound to $v$, replacing any earlier binding}{aux:update}
\end{tabular}
\end{center}
