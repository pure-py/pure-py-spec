\begin{figure}
\flushleft \shadebox{$\Gamma, x \importsR \theta$}
\begin{mathpar}
\small
\inferrule*[lab={\ruleName{wf-imp-member}}]
{
  \Gamma \vdash x : \theta
}
{
  \Gamma, x \importsR \theta
}
\and
\inferrule*[lab={\ruleName{wf-imp-submodule}}]
{
  \bind{x}{\modStub{q}} \in \Gamma \\
  \vdash_{\mathsf{load}} q : \Delta
}
{
  \Gamma, x \importsR \modLoaded{q}{\Delta}
}
\end{mathpar}

\caption{Name $x$ imported from module signature $\Gamma$ has entry $\theta$}
\label{fig:imports}
\end{figure}

\begin{figure}
\begin{subfigure}{\textwidth}
\flushleft \shadebox{$\vdash \seq{\iota} : \Gamma$}
\begin{mathpar}
\small
\inferrule*[lab={\ruleName{wf-imports-nil}}]
{
  \strut
}
{
  \vdash \varepsilon : \varnothing
}
\and
\inferrule*[lab={\ruleName{wf-import}}]
{
  q' = x \cons \seq{x} \\
  q \not\properPrefixOf q' \\
  \vdash_{\mathsf{load}} q' : \Delta \\
  q', \modLoaded{q'}{\Delta} \loadsAs \theta \\
  \vdash \seq{\iota} : \Gamma
}
{
  \vdash \exImport{q'} \cons \seq{\iota} : \set{\bind{x}{\theta}} \extend \Gamma
}
\and
\inferrule*[lab={\ruleName{wf-from-import}}]
{
  \vdash_{\mathsf{load}} q' : \Delta \\
  \vdash_{\mathsf{load}} q^\dagger : \Delta_{q^\dagger} \quad (\forall q^\dagger \properPrefixOf q',\; q^\dagger \not\prefixOf q) \\
  \Delta, x_i \importsR \theta_i \quad (\forall i) \\
  \vdash \seq{\iota} : \Gamma
}
{
  \vdash \exFromImport{q'}{x_1, \ldots, x_{n+1}} \cons \seq{\iota} : \set{\bind{x_1}{\theta_1}, \ldots, \bind{x_{n+1}}{\theta_{n+1}}} \extend \Gamma
}
\end{mathpar}

\subcaption{Import prefix $\seq{\iota}$ of module $q$ produces bindings $\Gamma$}
\end{subfigure}

\vspace{2mm}

\begin{subfigure}{\textwidth}
\flushleft \shadebox{$q, \theta \loadsAs \theta'$}
\begin{mathpar}
\small
\inferrule*[lab={\ruleName{wf-loads-as-simple}}]
{
  \strut
}
{
  x, \theta \loadsAs \theta
}
\and
\inferrule*[lab={\ruleName{wf-loads-as-qualified}}]
{
  \vdash_{\mathsf{load}} q : \Gamma' \\
  q, \modLoaded{q}{\Gamma' \extend \set{\bind{x}{\theta}}} \loadsAs \theta'
}
{
  q.x, \theta \loadsAs \theta'
}
\end{mathpar}
\subcaption{Reference $\theta$ for module $q$ loads as reference $\theta'$ for root of $q$, statically loading ancestors of $q$}
\end{subfigure}
\caption{Import well-formedness}
\label{fig:well-formed-imports}
\end{figure}

\begin{figure}
\flushleft \shadebox{$\vdash_{\mathsf{load}} q : \Gamma$}
\begin{mathpar}
\small
\inferrule*[lab={\ruleName{wf-predefined}}]
{
  \mathcal{P}(q) = (\rho, \Gamma)
}
{
  \vdash_{\mathsf{load}} q : \Gamma
}
\and
\inferrule*[lab={\ruleName{wf-module}}]
{
  \mathcal{M}(q) = \seq{\iota}\;t \\
  \classesOf(t) \text{ distinct} \\
  \vdash \seq{\iota} : \Gamma \\
  \mathcal{P}(\pyinline{builtins}) = (\rho, \Gamma') \\
  \Gamma' \mathbin{\override} \Gamma \vdash t : \Assigns{\Delta} \\
  (\dom(\Gamma) \cup \dom(\Delta)) \cap \dom(\submods(q)) = \varnothing
}
{
  \vdash_{\mathsf{load}} q : \submods(q) \override \Delta
}
\end{mathpar}

\flushleft \shadebox{$\vdash \mathcal{M}$}
\begin{mathpar}
\small
\inferrule*[lab={\ruleName{wf-program}}]
{
  \vdash_{\mathsf{load}} \pymath{\_\_main\_\_} : \Gamma
}
{
  \vdash \mathcal{M}
}
\end{mathpar}
\caption{Module well-formedness}
\label{fig:well-formed-module}
\end{figure}
