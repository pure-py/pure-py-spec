\begin{figure}
\small
\setlength{\jot}{2pt}
\flushleft \shadebox{$\odot(\sigma, \sigma') : ((\tau, \tau'), \tau^\dagger)$}
\begin{defalign}
& \odot(\sigma, \sigma') : ((\sigma, \sigma'), \tyBool) && \quad \text{if } \odot \in \set{\pyinline{==}, \pyinline{!=}} \text{ and } \sigma, \sigma' \text{ comparable equality types} \\
& \odot(\sigma, \tyList{\tau}) : ((\sigma, \tyList{\tau}), \tyBool) && \quad \text{if } \odot \in \set{\pyinline{in}, \pyinline{not in}} \text{ and } \sigma, \tau \text{ comparable} \\
& \odot(\sigma, \tyTuple{\seq{\tau}}) : ((\sigma, \tyTuple{\seq{\tau}}), \tyBool) && \quad \text{if } \odot \in \set{\pyinline{in}, \pyinline{not in}} \text{ and } \sigma, \joinTy(\seq{\tau}) \text{ comparable} \\
& \odot(\sigma, \sigma') : ((\tyStr, \tyStr), \tyBool) && \quad \text{if } \odot \in \set{\pyinline{in}, \pyinline{not in}} \text{ and } \sigma, \sigma' \subty \tyStr \\
& \odot(\sigma, \tyDict{\tau}) : ((\tyStr, \tyDict{\tau}), \tyBool) && \quad \text{if } \odot \in \set{\pyinline{in}, \pyinline{not in}} \text{ and } \sigma \subty \tyStr \\
& \odot(\sigma, \sigma') : ((\tyFloat, \tyFloat), \tyBool) && \quad \text{if } \odot \in \set{\pyinline{<}, \pyinline{<=}, \pyinline{>}, \pyinline{>=}} \text{ and } \sigma, \sigma' \subty \tyFloat \\
& \odot(\sigma, \sigma') : ((\tyStr, \tyStr), \tyBool) && \quad \text{if } \odot \in \set{\pyinline{<}, \pyinline{<=}, \pyinline{>}, \pyinline{>=}} \text{ and } \sigma, \sigma' \subty \tyStr \\
& \odot(\sigma, \sigma') : ((\tyInt, \tyInt), \tyInt) && \quad \text{if } \odot \in \set{\pyinline{+}, \pyinline{-}, \pyinline{*}, \pyinline{//}, \pyinline{\%}} \text{ and } \sigma, \sigma' \subty \tyInt \\
& \odot(\sigma, \sigma') : ((\tyFloat, \tyFloat), \tyFloat) && \quad \text{if } \odot \in \set{\pyinline{+}, \pyinline{-}, \pyinline{*}, \pyinline{//}, \pyinline{\%}, \pyinline{/}, \pyinline{**}} \text{ and } \sigma, \sigma' \subty \tyFloat \\
& \pyinline{+}(\sigma, \sigma') : ((\tyStr, \tyStr), \tyStr) && \quad \text{if } \sigma, \sigma' \subty \tyStr \\
& \rlap{$\pyinline{+}(\tyList{\sigma}, \tyList{\tau}) : ((\tyList{\sigma}, \tyList{\tau}), \tyList{\joinTy(\sigma, \tau)})$} \\
& \rlap{$\pyinline{+}(\tyTuple{\seq{\sigma}}, \tyTuple{\seq{\tau}}) : ((\tyTuple{\seq{\sigma}}, \tyTuple{\seq{\tau}}), \tyTuple{\seq{\sigma} \mathbin{\concat} \seq{\tau}})$} \\
& \pyinline{*}(\sigma, \sigma') : ((\tyStr, \tyInt), \tyStr) && \quad \text{if } \sigma \subty \tyStr \text{ and } \sigma' \subty \tyInt \\
& \pyinline{*}(\sigma, \sigma') : ((\tyInt, \tyStr), \tyStr) && \quad \text{if } \sigma \subty \tyInt \text{ and } \sigma' \subty \tyStr \\
& \pyinline{*}(\tyList{\tau}, \sigma) : ((\tyList{\tau}, \tyInt), \tyList{\tau}) && \quad \text{if } \sigma \subty \tyInt \\
& \pyinline{*}(\sigma, \tyList{\tau}) : ((\tyInt, \tyList{\tau}), \tyList{\tau}) && \quad \text{if } \sigma \subty \tyInt \\
& \pyinline{**}(\sigma, \tyLiteral{n}) : ((\tyInt, \tyLiteral{n}), \tyInt) && \quad \text{if } \sigma \subty \tyInt \text{ and } n \text{ an integer} \\
& \pyinline{**}(\sigma, \tyLiteral{-n}) : ((\tyInt, \tyLiteral{-n}), \tyFloat) && \quad \text{if } \sigma \subty \tyInt \text{ and } n \text{ a positive integer}
\end{defalign}

\vspace{2mm}

\flushleft \shadebox{$\ominus(\sigma) : (\tau, \tau^\dagger)$}
\begin{defalign}
& \pyinline{not}(\sigma) : (\tyBool, \tyBool) && \quad \text{if } \sigma \subty \tyBool \\
& \ominus(\sigma) : (\tyInt, \tyInt) && \quad \text{if } \ominus \in \set{\pyinline{+}, \pyinline{-}} \text{ and } \sigma \subty \tyInt \\
& \ominus(\sigma) : (\tyFloat, \tyFloat) && \quad \text{if } \ominus \in \set{\pyinline{+}, \pyinline{-}} \text{ and } \sigma \subty \tyFloat
\end{defalign}
\caption{Operator overloads}
\label{fig:operator-overloads}
\end{figure}
