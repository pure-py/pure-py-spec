\begin{figure}
\flushleft \shadebox{$\rho, x \importsR o$}
\begin{mathpar}
\small
\inferrule*[lab={\ruleName{eval-imp-member}}]
{
  \bind{x}{v} \in \rho \\
  v \neq \modStub{q'} \quad (\forall q')
}
{
  \rho, x \importsR \val{v}
}
\and
\inferrule*[lab={\ruleName{eval-imp-submodule}}]
{
  \bind{x}{\modStub{q'}} \in \rho \\
  q' \Rightarrow_{\mathsf{load}} \val{\rho'}
}
{
  \rho, x \importsR \val{\modLoaded{q'}{\rho'}}
}
\and
\inferrule*[lab={\ruleName{eval-imp-submodule-fail}}]
{
  \bind{x}{\modStub{q'}} \in \rho \\
  q' \Rightarrow_{\mathsf{load}} \aborts{\kappa}
}
{
  \rho, x \importsR \aborts{\kappa}
}
\end{mathpar}

\caption{Name $x$ imported from module environment $\rho$ has outcome $o$}
\label{fig:eval-imports}
\end{figure}

\begin{figure}
\begin{subfigure}{\textwidth}
\flushleft \shadebox{$\seq{\iota} \Rightarrow o$}
\begin{mathpar}
\small
\inferrule*[lab={\ruleName{eval-imports-nil}}]
{
  \strut
}
{
   \varepsilon \Rightarrow \val{\varnothing}
}
\and
\inferrule*[lab={\ruleName{eval-import}}]
{
  q' = x \cons \seq{x} \\
  q' \Rightarrow_{\mathsf{load}} \val{\rho} \\
  q', \modLoaded{q'}{\rho} \loadsAs v \\
   \seq{\iota} \Rightarrow o
}
{
   \exImport{q'} \cons \seq{\iota} \Rightarrow \mlet{\rho'}{o}{\val{(\set{\bind{x}{v}} \extend \rho')}}
}
\and
\inferrule*[lab={\ruleName{eval-import-fail}}]
{
  q' = x \cons \seq{x} \\
  q' \Rightarrow_{\mathsf{load}} \aborts{\kappa}
}
{
   \exImport{q'} \cons \seq{\iota} \Rightarrow \aborts{\kappa}
}
\and
\inferrule*[lab={\ruleName{eval-from-import}}]
{
  q' \Rightarrow_{\mathsf{load}} \val{\rho} \\
  q^\dagger \Rightarrow_{\mathsf{load}} \val{\rho_{q^\dagger}} \quad (\forall q^\dagger \properPrefixOf q',\; q^\dagger \not\prefixOf q) \\
  \rho, x_i \importsR \val{v_i} \quad (\forall i) \\
   \seq{\iota} \Rightarrow o
}
{
   \exFromImport{q'}{x_1, \ldots, x_{n+1}} \cons \seq{\iota} \Rightarrow \mlet{\rho^\dagger}{o}{\val{(\set{\bind{x_1}{v_1}, \ldots, \bind{x_{n+1}}{v_{n+1}}} \extend \rho^\dagger)}}
}
\and
\inferrule*[lab={\ruleName{eval-from-import-fail}}]
{
  q' \Rightarrow_{\mathsf{load}} \aborts{\kappa}
}
{
   \exFromImport{q'}{x_1, \ldots, x_{n+1}} \cons \seq{\iota} \Rightarrow \aborts{\kappa}
}
\and
\inferrule*[lab={\ruleName{eval-from-import-fail-ancestor}}]
{
  q' \Rightarrow_{\mathsf{load}} \val{\rho} \\
  q^\dagger \Rightarrow_{\mathsf{load}} \aborts{\kappa} \quad (\text{some } q^\dagger \properPrefixOf q',\; q^\dagger \not\prefixOf q)
}
{
   \exFromImport{q'}{x_1, \ldots, x_{n+1}} \cons \seq{\iota} \Rightarrow \aborts{\kappa}
}
\and
\inferrule*[lab={\ruleName{eval-from-import-fail-member}}]
{
  q' \Rightarrow_{\mathsf{load}} \val{\rho} \\
  q^\dagger \Rightarrow_{\mathsf{load}} \val{\rho_{q^\dagger}} \quad (\forall q^\dagger \properPrefixOf q',\; q^\dagger \not\prefixOf q) \\
  \rho, x_i \importsR \val{v_i} \quad (\forall i < j) \\
  \rho, x_j \importsR \aborts{\kappa}
}
{
   \exFromImport{q'}{x_1, \ldots, x_{n+1}} \cons \seq{\iota} \Rightarrow \aborts{\kappa}
}
\end{mathpar}

\subcaption{Import prefix $\seq{\iota}$ of module $q$ evaluates to outcome $o$}
\end{subfigure}

\vspace{2mm}

\begin{subfigure}{\textwidth}
\flushleft \shadebox{$q, v \loadsAs v'$}
\begin{mathpar}
\small
\inferrule*[lab={\ruleName{eval-loads-as-simple}}]
{
  \strut
}
{
  x, v \loadsAs v
}
\and
\inferrule*[lab={\ruleName{eval-loads-as-qualified}}]
{
  q \Rightarrow_{\mathsf{load}} \val{\rho} \\
  q, \modLoaded{q}{\rho \extend \set{\bind{x}{v}}} \loadsAs v'
}
{
  q.x, v \loadsAs v'
}
\end{mathpar}
\subcaption{Reference $v$ for module $q$ loads as reference $v'$ for root of $q$, loading ancestors of $q$}
\end{subfigure}
\caption{Import evaluation}
\label{fig:eval-import-prefix}
\end{figure}

\begin{figure}
\flushleft \shadebox{$q \Rightarrow_{\mathsf{load}} o$}
\begin{mathpar}
\small
\inferrule*[lab={\ruleName{eval-predefined}}]
{
  \mathcal{P}(q) = (\Gamma, \rho)
}
{
  q \Rightarrow_{\mathsf{load}} \val{\rho}
}
\and
\input{spec/rules/eval-module}
\and
\inferrule*[lab={\ruleName{eval-module-fail}}]
{
  \mathcal{M}(q) = \seq{\iota}\;t \\
   \seq{\iota} \Rightarrow \aborts{\kappa}
}
{
  q \Rightarrow_{\mathsf{load}} \aborts{\kappa}
}
\and
\inferrule*[lab={\ruleName{eval-module-fail-body}}]
{
  \mathcal{M}(q) = \seq{\iota}\;t \\
   \seq{\iota} \Rightarrow \val{\rho} \\
  \mathcal{P}(\pyinline{builtins}) = (\Gamma', \rho') \\
  \rho' \mathbin{\override} \rho, t \Rightarrow \aborts{\kappa}
}
{
  q \Rightarrow_{\mathsf{load}} \aborts{\kappa}
}
\end{mathpar}

\flushleft \shadebox{$\mathcal{M} \Rightarrow \kappa$}
\begin{mathpar}
\small
\input{spec/rules/eval-program}
\and
\input{spec/rules/eval-program-abort}
\end{mathpar}
\caption{Module and program evaluation}
\label{fig:eval-modules}
\end{figure}
