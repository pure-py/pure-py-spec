% Rows of the auxiliary-functions index; the auxindex environment supplies the table.
\auxgroup{Syntactic analyses}
\auxrow{$\assignsS(t)$}{set of variables}{variables \added{declared or }assigned in $t$, not descending into nested functions}{aux:assigns}
\auxrow{\added{$\declaresS(t)$}}{\added{sequence of variables}}{\added{names declared in $t$, in order and with repeats, not descending into nested functions}}{aux:declares}
\auxrow{$\binds(p)$, $\binds(\seq{g})$\added{, $\binds(s)$}}{set of variables}{variables bound by pattern $p$, \deleted{or }by generators of qualifiers $\seq{g}$\added{, or by patterns within statement $s$}}{aux:binds}
\auxrow{$\replaced{\captures}{\capturesE}(e)$}{set of variables}{variables of enclosing scope captured by lambdas in $e$}{aux:captures-e}
\auxrow{\replaced{$\captures(\seq{g})$}{$\captures(t)$, $\captures(\seq{g})$}}{set of variables}{\replaced{variables of enclosing scope captured by lambdas in qualifiers $\seq{g}$}{variables of enclosing scope captured by lambdas and nested functions in $t$, or in $\seq{g}$}}{aux:captures}
\specauxrow{\deleted{$\fvS(s)$}}{\deleted{set of variables}}{\deleted{free variables of statement $s$}}{aux:fv}
\specauxrow{$\fvE(e)$\added{, $\fvE(\seq{e})$}}{set of variables}{free variables of expression $e$\added{, or of each expression in $\seq{e}$}}{aux:fv-e}
\noalign{\vskip 2mm}
\auxgroup{Types}
\auxrow{$\baseType(\ell)$, $\baseType(\tau)$}{type}{base type of literal $\ell$, or of type $\tau$}{aux:base-type}
\auxrow{$\joinTy{\Sigma}{\sigma}{\tau}$, $\bigJoinTy{\Sigma}{\seq{\tau}}$}{type}{larger type if comparable, and union otherwise}{aux:join}
\auxrow{$\meetTy{\Sigma}{\sigma}{\tau}$}{type}{greatest type below both}{aux:meet}
\auxrow{$\elemTy(\Sigma, \tau)$}{type}{type of elements drawn from $\tau$ by generator}{aux:elem-type}
\auxrow{$\shapes(\Sigma, \tau, H)$, $\shapes(\Sigma, \seq{\tau})$}{ordered set of shapes or of shape sequences}{shapes of $\tau$ once heads in $H$ are excluded; sequences of shapes of $\seq{\tau}$}{aux:shapes}
\auxrow{$\shapeTy(k)$}{type}{least type of shape $k$}{aux:shape-type}
\auxrow{$\seqSafe(\Sigma, \Gamma, p, \tau)$}{Boolean}{tuple patterns in $p$ never checked against lists, list patterns never against tuples}{aux:seq-safe}
\auxrow{\added{$\resolveOp(\Sigma, \odot, \sigma_1, \sigma_2)$, $\resolveOp(\Sigma, \ominus, \sigma)$}}{\added{type}}{\added{result type of most specific overload at the operand types}}{aux:resolve-op}
