\section{Syntax}
\label{sec:syntax}

The syntax of \PurePy is given in \figrefThree{syntax-stmt}{syntax-expr}{syntax-types}. \figref{metavariables} summarises the metavariables used throughout.
A sequence $\seq{x}$ denotes $x_1, \ldots, x_n$ for some $n$; inside the brackets of a tuple, list, type
application or argument list it stands for the comma-separated elements, so $\exTuple{\seq{e}}$ is
$\exTuple{e_1, \ldots, e_n}$. The sequence $x \cons \seq{x}$ has head $x$ and tail $\seq{x}$, and
$\seq{x} \mathbin{\concat} \seq{y}$ is concatenation.
Comma-separated elements inside set braces are pairwise distinct, so $\set{x_1, \ldots, x_n}$ has $n$
elements; the set of the elements of a sequence, $\set{\seq{x}}$, assumes no distinctness. A sequence
comprehension $(x_i \mid i \in I)$, where the finite index set $I$ carries a total order, denotes the
sequence of the $x_i$ in that order. When the order of its elements matters, a set $\set{x_1, \ldots, x_n}$ is
\emph{ordered} by position. A comprehension over an ordered set preserves the order of its source;
$X \setminus Y$ keeps the surviving elements of $X$ in their order; $X \disjunion Y$, defined when $X$
and $Y$ are disjoint, appends the elements of $Y$ to those of $X$; $X \cup Y$ appends the elements of
$Y$ not in $X$; and a product of ordered sets is ordered lexicographically.

\paperinput{spec/fig/metavariables}

\subsection{Lexical syntax}
\label{sec:lexical}

The grammar of \PurePy is an abstract syntax, and its metavariables range over values rather than
over source text: $n$ is a number and $w$ is a string, not the lexemes that denote them. A number is never
negative, since Python writes a negative number as unary minus applied to a literal, and a length or an
exit status is an integer. The translation of source text
into abstract syntax follows Python, including the indentation that delimits blocks. \Added{A list of type
parameters or type arguments is always present in the abstract syntax and may be empty; in source the
brackets are omitted with the list, since Python admits no empty brackets, and the empty tuple type is
written \pyinline{tuple[()]}.}

\subsection{Contexts}
\label{sec:contexts}

Metavariables $x$, $f$\Replaced{, $c$ and $\alpha$}{ and $c$} range over \emph{variables}; idiomatically $f$ is used when the variable is expected to denote a function\Replaced{, $c$ when it is expected to denote a class and $\alpha$ when it is a type parameter}{ and $c$ when it is expected to denote a class}. A \emph{qualified name} $q$ is a non-empty sequence of variables separated by dots (\figref{syntax-stmt}); a variable is thus a qualified name with a single component.

\begin{definition}[Context]
\label{def:context}
A context $\Gamma$ (or $\Delta$) is a finite map from variables to \emph{context entries} $\theta$, which
take the following forms:

{\normalfont
\begin{center}
\small
\begin{tabular}{>{\raggedright\arraybackslash}p{0.17\textwidth}>{\raggedright\arraybackslash}p{0.155\textwidth}>{\raggedright\arraybackslash}p{0.555\textwidth}}
\arrayrulecolor{gray!60}\hline
\rowcolor{grammarheadcolor}Entry & Form & Records \\
variable & $\tau$, $\DU{\tau}$, $\PU{\tau}$ or $\Unbound$ & type of definitely assigned variable; $\DU{\tau}$ if declared at $\tau$ and definitely unassigned, on no path; $\PU{\tau}$ if declared at $\tau$ and possibly unassigned, on some paths only; $\Unbound$ if its binding statement has not been reached \\
module stub & $\modStub{q}$ & module $q$, named but not loaded \\
module reference & $\modLoaded{q}{\Gamma}$ & module $q$ with signature $\Gamma$ \\
class entry & $\Type{C}$ & name denotes class $C$ \\
predefined name & $\predefName$ & name usable only in annotation or as decorator \\
\end{tabular}
\end{center}
}
\end{definition}

\begin{definition}[Class]
\label{def:class}
A class $C$ is a marked name $\fq{q.c}$: a module $q$ and a class name $c$ declared in $q$. A module
declares each class name at most once, so the fully qualified name of a class is the identity of the
class. A class is also the type of its instances.
\end{definition}

\begin{definition}[Class table]
\label{def:class-table}
A class table $\Sigma$ is a finite map from classes to class table entries, with $\Sigma(C)$ of the form
$\clsRec{\bind{\seq{x}}{\seq{\tau}}}{D}$: the distinct fields $\seq{x}$ of $C$ with their declared types
$\seq{\tau}$, and its base class $D$, or $\bot$ if it has none. Every class named in $\Sigma$ is in
$\dom(\Sigma)$, and the relation taking each class of $\Sigma$ to its base class is
acyclic.
\end{definition}

\noindent The class table is borrowed from Featherweight Java~\cite{igarashi2001}. Module references arise from import statements. A predefined name is a
member such as $\pyinline{Callable}$ or $\pyinline{dataclass}$ that no expression may mention: it names a
type, heads a type form, or decorates a class declaration. The definite-assignment terminology is borrowed from Java~\cite[\S16]{gosling2025}: a variable bound to a type is \emph{definitely assigned}; $\DU{\tau}$ records a variable declared at $\tau$ and \emph{definitely unassigned}, on no path, as a blank final in Java before its assignment; $\PU{\tau}$ one \emph{possibly unassigned}, on some paths only, so neither definitely assigned nor definitely unassigned; and $\Unbound$ a name whose binding statement has not been reached, which Python reports as an unbound local.
It is useful to represent contexts as total functions on the set of all variables by extending the codomain with $\bot$ to indicate \emph{undefined}. We write $\Gamma, \bind{x}{\theta}$ for the context that sends $x$ to $\theta$ and otherwise agrees with $\Gamma$, define $\dom(\Gamma) = \set{x \mid \Gamma(x) \neq \bot}$, and write $\varnothing$ for the context in which all variables are undefined.

\subsubsection{Context operations}
\label{sec:context-operations}

Contexts admit a non-commutative monoid of sequential composition $\Gamma \override \Delta$ (``override'') and
an idempotent commutative semigroup of parallel composition $\merge$ (``merge''). Together these form a
\emph{right near-semiring}~\cite{vanhoorn1967}: $\override$ right-distributes over $\merge$, but left
distributivity does not hold. Both are defined pointwise (\figref{context-ops}): override is right-biased
and has $\bot$ as both left and right unit; merge is defined only on variable entries, returning the
entry when both arguments are equal and $\PU{\tau}$ otherwise, for the declared type $\tau$ the arguments carry, so $\bot$ is not a unit.
\input{spec/fig/context-ops}

A third operation, \emph{context extension} $\Gamma \extend \Delta$ (also \figref{context-ops}),
combines module references for the same module recursively, preferring loaded references to stubs, and is
otherwise right-biased like override. It is used in processing an import prefix, in which two imports with the same root, such as
\pyinline{a.b} and \pyinline{a.c}, both bind the root and must combine rather than shadow.

\subsection{Expressions and statements}
\label{sec:expressions-statements}

Statements are sequenced by
juxtaposition, $s\;s'$; a function body, a branch of a conditional and the body of a case are statements.
A \emph{top-level statement} $t$ is a statement\Replaced{, a class declaration or a \pyinline{type} statement}{ or a class declaration}, also sequenced by juxtaposition; the body of a
module after its imports is a top-level statement, so class declarations \Added{and \pyinline{type} statements} occur only at the top level of a
module. \Added{A function definition, a class declaration and a \pyinline{type} statement bind \emph{type parameters}
$\seq{\alpha}$, written in brackets after the name as in Python 3.12. A \pyinline{type} statement declares a
\emph{type alias}, and the base class of a dataclass is a qualified name applied to type arguments.} A \emph{mutual region} $d_1 \ldots d_{n+1}$ is a maximal
run of consecutive \pyinline{def} statements in a sequence: any other statement separates mutual
regions. Maximality is not enforced by the grammar in \figref{syntax-stmt}; the parser is responsible for
grouping consecutive \pyinline{def}s into a single mutual region. The well-formedness rules treat each
mutual region as one mutually-recursive binding\paperonly{ (see \secref{mutual-recursion})}.

\input{spec/fig/syntax-stmt}
\input{spec/fig/syntax-expr}
\input{spec/fig/syntax-types}

An \emph{argument list} $a$ (\figref{syntax-expr}) is a sequence of positional arguments followed by
keyword arguments $x = u$, where an argument $u$ is an expression in a constructor call and a pattern in a
constructor pattern. \Added{A constructor call also passes type arguments to the class; a constructor pattern
cannot, since Python admits only a name in a class pattern.} The auxiliary function $\fieldMap$
(\paperonly{\figref{auxiliary-functions}}\speconly{\S\ref{aux:field-map}}) pairs each field of the class
with an argument: the positional arguments fill the leading fields in order, and a keyword argument $x = u$
fills the field $x$. It is undefined unless every field receives exactly one argument.

\paragraph{Type annotations.}

Parameters, returns and dataclass fields are annotated with type expressions $\psi$, and an assignment
may be annotated (\figref{syntax-types}). A type expression names a class \Added{or a type alias} by a qualified name\Added{
applied to type arguments, and a type parameter by its variable}; a marked name
$\fq{q}$ is fully qualified, and does not occur in source. A \emph{type} $\tau$ is a type expression whose
class names are all marked\Added{ and which does not mention any alias}; the type system resolves a type expression into a type\speconly{ (\secref{types})}. We write $\bind{\seq{x}}{\seq{\psi}}$ for the parameter list $\bind{x_1}{\psi_1}, \ldots,
\bind{x_n}{\psi_n}$. The well-formedness rules and operational semantics do not consult annotations, which are given
meaning by the type system.

\subsubsection{Operators}
\label{sec:syntax-operators}

The metavariable $\odot$ ranges over the supported binary operators other than \pyinline{and} and
\pyinline{or}, and $\ominus$ over the supported unary
operators, both enumerated in \figref{precedence}. The operators \pyinline{and} and \pyinline{or} are distinct
expression forms, since they need not evaluate both operands. The grammar of \figref{syntax-expr} is intentionally ambiguous
on operator forms; disambiguation is by the precedence and associativity given in \figref{precedence},
which follow Python.

\input{spec/fig/precedence}

\subsection{Modules and programs}
\label{sec:modules-programs}

A \PurePy \emph{program} is a finite map $\mathcal{M}$ from \emph{fully-qualified module names}
(\figref{syntax-stmt}) to \emph{module bodies} $\mu$, of which one is a distinguished \emph{main module} named
$\pymath{\_\_main\_\_}$; here $\nameOf(q)$ is the string literal naming $q$, so
$\nameOf(\pymath{\_\_main\_\_}) = \pymath{"\_\_main\_\_"}$. We identify a module with its fully-qualified name, writing ``module $q$'' for the module named $q$. A module body is a sequence of imports
$\seq{\iota}$, its \emph{import prefix}, followed by a top-level statement $t$; since imports are not statements, they can appear neither
within a statement nor after it. The statement begins with the
assignment $\exAssign{\pymath{\_\_name\_\_}}{\nameOf(q)}$, which binds the name of the module when the module
loads (\secref{well-formedness-modules}).

A second map $\mathcal{P}$ gives the
predefined modules of \figref{predefined-values}, which have no body: $\mathcal{P}$ takes each such name to its
environment and context (Definition~\ref{def:predefined}). The two domains are disjoint, and their union is
prefix-closed: the parent of any module in $\dom(\mathcal{M}) \cup \dom(\mathcal{P})$ is itself in
$\dom(\mathcal{M}) \cup \dom(\mathcal{P})$.\footnote{A source tree encodes such an $\mathcal{M}$: its modules are the \texttt{.py} files and the
directories, each named by its path, so the domain is prefix-closed by construction. The statement of a module is
the statement sequence of its \texttt{.py} file, or of its \texttt{\_\_init\_\_.py} for a directory, preceded by the
$\pymath{\_\_name\_\_}$ assignment; a directory without an \texttt{\_\_init\_\_.py} (a namespace package,
PEP 420, \url{https://peps.python.org/pep-0420/}) contributes that assignment alone.} The maps $\mathcal{M}$ and $\mathcal{P}$ are fixed and ambient in all
definitions that follow.

\begin{definition}[Predefined contexts and environments]
\label{def:predefined}
$\mathcal{P}$ takes each module $q$ of \figref{predefined-values} to a pair $(\Gamma, \rho)$, where $\Gamma$
binds $\pymath{\_\_name\_\_}$ at $\tyStr$ and each member of $q$ at the context entry listed in \figref{predefined-signatures}, and $\rho$ binds $\pymath{\_\_name\_\_}$ to $\nameOf(q)$ and each member to the corresponding value
(\figref{predefined-values}). These signatures name types
that are themselves members, sometimes of another predefined module, so each is well-formed in the union of
the predefined signatures.
\end{definition}

\input{spec/fig/predefined-signatures}

\input{spec/fig/predefined-values}

Write $q \prefixOf q'$ (\emph{prefix}) when $q' = q$ or $q' = q.q^\dagger$ for some $q^\dagger$, and $\properPrefixOf$ for
the strict order; the proper prefixes of a module name are its \emph{containing modules}, and it is
\emph{contained} in each of them.\speconly{ The metafunctions
on modules and classes are defined in \secref{auxiliary-classes}.}
\paperinput{paper/syntax/classes}
