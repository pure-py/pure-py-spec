\subsection{Conformance suite}
\label{sec:testing}

The conformance suite pairs each test with the verdict Python itself gives, and runs on CPython,
GraalPy, and Pyodide. \todo{Diagram showing the various kinds of test.} \todo{Reference interpreter, needed
to detect dynamically excluded operations, which a stock interpreter simply runs as Python (\issue{64}).}
