\section{Conformance}
\label{sec:conformance}

This document defines \PurePy version \versionstring. \secref{syntax} gives the abstract syntax; lexical
syntax is Python's (\secref{lexical}). \secref{well-formedness} defines well-formedness, the static part of
the definition, and \secref{opsem} the operational semantics, which is defined for well-formed programs and
assigns each valid run its result. Python means Python 3.12 throughout.

\input{spec/conformance/compatibility}

\input{spec/conformance/suite}
