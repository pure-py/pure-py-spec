\inferrule*[lab={\ruleName{eval-pat-constr}}]
{
  \resolveClass(\rho, q) = C \\
  C \in \ancestors(C') \\
  \rho, \fieldMap(C, \seq{p})(x), \rho^\dagger(x) \matches m_x \quad (\forall x \in \fields(C))
}
{
  \rho, \patConstr{q}{\seq{p}}, \exObj{\Gamma'}{C'}{\rho^\dagger} \matches {\textstyle\bigotimes_{x \in \fields(C)}}\, m_x
}
