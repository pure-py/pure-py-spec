\inferrule*[lab={\ruleName{eval-pat-constr}}]
{
  \resolveClass(\rho, q) = C \\
  \added{C = \clsEntry{q'}{\seq{x}}{A}} \\
  \replaced{q' \in \ancestors(C')}{C \in \ancestors(C')} \\
  \rho, \fieldMap(C, \seq{p})(x), \rho'(x) \matches m_x \quad (\forall x \in \fields(C))
}
{
  \rho, \patConstr{q}{\seq{p}}, \exObj{\Gamma'}{C'}{\rho'} \matches {\textstyle\bigotimes_{x \in \fields(C)}}\, m_x
}
