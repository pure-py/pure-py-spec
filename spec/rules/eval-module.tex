\inferrule*[lab={\ruleName{eval-module}}]
{
  \mathcal{M}(q) = \seq{\iota}\;t \\
  \mathcal{P}(\pyinline{builtins}) = \replaced{(\Gamma, \rho)}{(\rho', \Gamma')} \\
  q, \seq{\iota} \Rightarrow \val{\replaced{\rho'}{\rho}} \\
  q, \replaced{\rho}{\rho'} \mathbin{\override} \replaced{\rho'}{\rho} \mathbin{\added{\override}} \added{\scopeS(\set{\declaresS(\seq{\iota})}, t)}, t \Rightarrow \assigns{\rho^\dagger}
}
{
  q \Rightarrow_{\mathsf{load}} \val{(\submods(q) \override \rho^\dagger)}
}
