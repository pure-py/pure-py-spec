\inferrule*[lab={\ruleName{eval-module}}]
{
  \mathcal{M}(q) = \seq{\iota}\;t \\
  q, \seq{\iota} \Rightarrow \val{\rho} \\
  \mathcal{P}(\pyinline{builtins}) = (\rho', \Gamma') \\
  q, \rho' \mathbin{\override} \added{\set{\bind{x}{\Unbound} \mid x \in \assignsS(t)} \override} \rho, t \Rightarrow \assigns{\rho^\dagger}
}
{
  q \Rightarrow_{\mathsf{load}} \val{(\submods(q) \override \rho^\dagger)}
}
