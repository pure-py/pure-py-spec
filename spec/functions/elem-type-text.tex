Type of the elements drawn from $\tau$ by a generator, mirroring $\iterOp$ (\secref{auxiliary-values}). A
tuple is iterated at the join of the base types of its components, and a type with no clause cannot be
iterated.
