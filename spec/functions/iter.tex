\iterOp(\exObj{\Gamma}{C}{\rho}) &{}= 0 \cons \ldots \cons \rho(\pyinline{stop})-1 && \text{if } C \text{ is } \rangeClass \text{ and } \rho(\pyinline{stop}) > 0 \\*
\iterOp(\exObj{\Gamma}{C}{\rho}) &{}= \varepsilon && \text{if } C \text{ is } \rangeClass \\*
\iterOp(\delta) &{}= w_1 \cons \ldots \cons w_n && \text{where } \delta = \bind{w_1}{v_1} \cons \ldots \cons \bind{w_n}{v_n} \\*
\iterOp(v) &{}= \elems(v) && \text{otherwise}
