\added{\wellScoped(Y, s)} &\added{{}= \True} && \added{\text{if } \declaresS(s) \text{ distinct and } Y, \set{\declaresS(s)}, \binds(s) \text{ pairwise disjoint}} \\*
\added{\wellScoped(Y, s)} &\added{{}= \False} && \added{\text{otherwise}}
