True unless a sequence pattern within $p$ would be matched against a value of the other kind at type
$\tau$: a tuple pattern agrees with no list type and a list pattern with no tuple type, a pattern agrees
with a union when it agrees with both members, and a sub-pattern must agree at the type of the position it
is matched at.
