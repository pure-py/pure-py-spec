Smaller of two comparable types. Where $\sigma$ and $\tau$ are incomparable, a meet distributes over a
union and takes tuples of the same arity componentwise; every other pair meets at \pyinline{Never}.
