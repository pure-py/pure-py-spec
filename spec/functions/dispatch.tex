\dispatch(\rho, v, \varepsilon, \varepsilon) &= (\varnothing, \exPass)\vphantom{\seq{p}} \\*
\dispatch(\rho, v, p \cons \seq{p}, s \cons \seq{s}) &= (\rho', s) && \text{if } \rho, p, v \matches \matchOk{}\rho' \\*
\dispatch(\rho, v, p \cons \seq{p}, s \cons \seq{s}) &= \dispatch(\rho, v, \seq{p}, \seq{s}) && \text{if } \rho, p, v \matches \noMatch
