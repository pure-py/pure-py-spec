Argument supplied for each field of $C$: positional arguments fill the fields in order and keyword
arguments match by name. Defined if and only if the arguments cover the fields exactly, so constructor
patterns and expressions check and evaluate field by field without regard to the split of the arguments
between positional and keyword.
