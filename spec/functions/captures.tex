\captures(\varepsilon) &= \varnothing \\*
\captures(\qualGuard{e} \cons \seq{g}) &= \captures(e) \cup \captures(\seq{g}) \\*
\captures(\qualGenerator{x}{e} \cons \seq{g}) &= \captures(e) \cup (\captures(\seq{g}) \setminus \set{x})
