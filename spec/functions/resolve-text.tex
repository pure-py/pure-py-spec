Value denoted by name $q$ in $\rho$, mirroring static judgement $\Gamma \vdash q : \theta$
(\figref{resolve-name}): a simple name is looked up in $\rho$ directly, and a qualified name in the
environment of the loaded module denoted by its prefix. Undefined if the prefix denotes a module stub or a
value other than a module.
