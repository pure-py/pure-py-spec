Shapes of type $\tau$ that remain once the heads in $N$ are excluded, one per member of a union and none
at all where $N$ exhausts $\tau$. A row of types yields the rows of shapes of its components, so a tuple
type has one shape per such row.
