Shapes of type $\tau$ that remain once the heads in $H$ are excluded, one per member of a union and none
at all where $H$ exhausts $\tau$. A sequence of types yields the sequences of shapes of its components.
