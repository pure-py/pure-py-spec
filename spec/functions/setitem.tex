\setitem(\varepsilon, w, v) &{}= \bind{w}{v} \\*
\setitem(\bind{w}{u} \cons \delta, w, v) &{}= \bind{w}{v} \cons \delta \\*
\setitem(\bind{w'}{u} \cons \delta, w, v) &{}= \bind{w'}{u} \cons \setitem(\delta, w, v) && \text{if } w \neq w'
