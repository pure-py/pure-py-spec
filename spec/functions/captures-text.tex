The variables of the enclosing scope captured by lambdas and nested function definitions within
$s$. On the cases not shown it takes the union of $\capturesE$ over the subexpressions of $s$ and of
$\capturesS$ over its sub-statements, and on a module body acts as $\assignsS$ does.
