\resolve(\odot, \sigma, \sigma') &{}= \tau^\dagger && \text{if } ((\tau, \tau'), \tau^\dagger) \text{ least in } \overloadsOf(\odot, \sigma, \sigma') \\*
\resolve(\odot, \sigma, \sigma') &{}= \tau^\dagger && \text{if } \overloadsOf(\odot, \sigma, \sigma') = \varnothing \text{ and} \\*
& && \qquad ((\tau, \tau'), \tau^\dagger) \text{ least in } \overloadsOf(\odot, \baseType(\sigma), \baseType(\sigma')) \\*[2mm]
\resolve(\ominus, \sigma) &{}= \tau^\dagger && \text{if } (\tau, \tau^\dagger) \text{ least in } \overloadsOf(\ominus, \sigma) \\*
\resolve(\ominus, \sigma) &{}= \tau^\dagger && \text{if } \overloadsOf(\ominus, \sigma) = \varnothing \text{ and} \\*
& && \qquad (\tau, \tau^\dagger) \text{ least in } \overloadsOf(\ominus, \baseType(\sigma))
