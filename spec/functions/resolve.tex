\resolve(\odot, \sigma, \sigma') &{}= \tau && \text{if } \odot : (\sigma, \sigma') \to \tau \\*
\resolve(\odot, \sigma, \sigma') &{}= \tau && \text{if } \odot : (\baseType(\sigma), \baseType(\sigma')) \to \tau \\*[2mm]
\resolve(\ominus, \sigma) &{}= \tau && \text{if } \ominus : \sigma \to \tau \\*
\resolve(\ominus, \sigma) &{}= \tau && \text{if } \ominus : \baseType(\sigma) \to \tau
