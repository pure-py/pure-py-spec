\resolve(\odot, \sigma, \sigma') &{}= \tau && \text{if } \odot : (\sigma, \sigma') \to \tau \\*
\resolve(\odot, \sigma, \sigma') &{}= \tau && \text{if } \odot : (\widen(\sigma), \widen(\sigma')) \to \tau \\*[2mm]
\resolve(\ominus, \sigma) &{}= \tau && \text{if } \ominus : \sigma \to \tau \\*
\resolve(\ominus, \sigma) &{}= \tau && \text{if } \ominus : \widen(\sigma) \to \tau
