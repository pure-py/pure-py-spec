\resolve(\odot, \sigma, \sigma') &{}= \tau^\dagger && \text{if } ((\tau, \tau'), \tau^\dagger) \text{ least in } \overloadsOf(\odot, \sigma, \sigma') \\*
\resolve(\ominus, \sigma) &{}= \tau^\dagger && \text{if } (\tau, \tau^\dagger) \text{ least in } \overloadsOf(\ominus, \sigma)
