\elemTy(\tyList{\tau}) &{}= \tau \\*
\elemTy(\tyStr) &{}= \tyStr \\*
\elemTy(q) &{}= \tyInt && \text{if } q = \pyinline{builtins}.\pyinline{range} \\*
\elemTy(\tyDict{\tau}) &{}= \tyStr \\*
\elemTy(\tyTuple{\seq{\tau}}) &{}= \joinTy(\widen(\tau_1), \ldots, \widen(\tau_n))
