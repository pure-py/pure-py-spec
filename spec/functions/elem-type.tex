\elemTy(\tyList{\tau}) &{}= \tau \\*
\elemTy(\tyStr) &{}= \tyStr \\*
\elemTy(\tyDict{\tau}) &{}= \tyStr \\*
\elemTy(\tyTuple{\seq{\tau}}) &{}= \joinTy(\baseType(\tau_1), \ldots, \baseType(\tau_n)) \\*
\elemTy(\tyUnion{\sigma}{\tau}) &{}= \joinTy(\elemTy(\sigma), \elemTy(\tau))
