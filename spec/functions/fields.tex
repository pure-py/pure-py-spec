\fields(\Sigma, C) &= \bind{\seq{x}}{\seq{\tau}} && \text{if } \Sigma(C) = \clsRec{\bind{\seq{x}}{\seq{\tau}}}{\bot} \\*
\fields(\Sigma, C) &= \fields(\Sigma, D) \mathbin{\concat} \bind{\seq{x}}{\seq{\tau}} && \text{if } \Sigma(C) = \clsRec{\bind{\seq{x}}{\seq{\tau}}}{D}
