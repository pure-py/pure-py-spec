Subscripting: the value under key $v'$ of a dictionary, or the element at index $v'$ of a sequence,
counting from the end when the index is negative. It aborts with $\errKey$,
$\errIndex$ or $\errType$ where Python raises the exception of that name.
