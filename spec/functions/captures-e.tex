\captures(\exLambda{\seq{x}}{e}) &= \fvE(e) \setminus \set{\seq{x}} \\*
\captures(\exListComp{e}{\seq{g}}) &= \captures(\seq{g}) \cup (\captures(e) \setminus \binds(\seq{g})) \\*
\captures(\exDictComp{e}{e'}{\seq{g}}) &= \captures(\seq{g}) \cup (\captures((e, e')) \setminus \binds(\seq{g}))
