The variables assigned anywhere in $t$, not descending into nested functions, which have their own scope. It is empty on the statements not shown and is the union over the branches of a
conditional, and on a module body $\seq{\iota}\;t$ acts on $t$.
