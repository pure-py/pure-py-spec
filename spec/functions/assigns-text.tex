The variables assigned anywhere in $s$, not descending into nested functions, which have their own scope. It is empty on the statements not shown and is the union over the branches of a
conditional; on a module body it acts on the optional statement, and is empty where there is none.
