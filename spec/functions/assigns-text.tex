The variables assigned anywhere in $s$, not descending into nested functions, which have their own scope. It is empty on the statements not shown and is the union over the branch blocks of a
conditional. It extends by union from statements to blocks, with the singleton block identified with its
statement, and on a module body $\seq{\iota}\;\seq{s}$ acts on $\seq{s}$.
