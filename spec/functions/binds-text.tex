Variables introduced by pattern $p$, by the generators of qualifier sequence $\seq{g}$, or by the patterns
within statement $s$, not descending into nested functions. On the cases
not shown it recurses structurally, taking the union over the immediate subpatterns, and on a
statement is the union over the branches of a conditional and empty on the statements not shown.
