\added{Whether the names declared in $s$, the body of a scope, are distinct from one another, from the names $Y$ and from the variables bound by patterns in $s$, where $Y$ is the set of parameters of a function or of imported names of a module.}
