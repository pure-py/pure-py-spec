\widen(n) &{}= \tyInt && \text{if } n \text{ is an integer literal} \\*
\widen(n) &{}= \tyFloat && \text{if } n \text{ is a float literal} \\*
\widen(w) &{}= \tyStr \\*
\widen(\pyinline{True}) = \widen(\pyinline{False}) &{}= \tyBool \\*
\widen(\pyinline{None}) &{}= \tyNone \\*[2mm]
\widen(\tyLiteral{\ell}) &{}= \widen(\ell) \\*
\widen(\tau) &{}= \tau && (\text{otherwise})
