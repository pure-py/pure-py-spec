\fieldMap(\Sigma, C, (u_1, \ldots, u_n, y_1 \mathord{=} u_{n+1}, \ldots, y_m \mathord{=} u_{n+m}))
   &{}= \set{\bind{x_1}{u_1}, \ldots, \bind{x_n}{u_n}, \bind{y_1}{u_{n+1}}, \ldots, \bind{y_m}{u_{n+m}}} \\*
   &\qquad \text{if } \replaced{\fields(\Sigma, C) = \scheme{\seq{\alpha}}{\bind{x_1}{\sigma_1} \cons \ldots \cons \bind{x_n}{\sigma_n} \cons \bind{\seq{y}\,'}{\seq{\sigma}\,'}}}{\dom(\fields(\Sigma, C)) = x_1 \cons \ldots \cons x_n \cons \seq{y}\,'} \\*
   &\qquad \text{and } \set{\seq{y}} = \set{\seq{y}\,'}
