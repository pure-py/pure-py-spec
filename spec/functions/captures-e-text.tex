The variables of the enclosing scope captured by lambdas within expression $e$, or within qualifier
sequence $\seq{g}$; the generator variables of a comprehension are local to it. On the cases not shown it
recurses structurally, taking the union over the immediate subexpressions.
