Fields of class $C$ with their declared types: the fields of its base class, if any, \Added{instantiated at the type arguments of the base,} followed by the
fields declared by $C$\Added{; the types may mention the type parameters of $C$, and the fields of an instantiated class $C[\seq{\tau}]$ have $\seq{\tau}$ substituted for them}. Field names are distinct, so the sequence is read as a map by rules that use only the names or only
the types.
