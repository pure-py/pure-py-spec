\Replaced{Fields of class $C$ with their declared types, as a field scheme over the type parameters of $C$: the fields of
its base class, if any, instantiated at the type arguments of the base, followed by the fields declared by $C$.
An instantiated class $C[\seq{\tau}]$ has the fields $\tyInst{\fields(\Sigma, C)}{\seq{\tau}}$. Field names are distinct,
so the bindings are read as a map when a rule looks up the type of a field}{Fields of class $C$ with their declared types: the fields of its base class, if any, followed by the
fields declared by $C$. Field names are distinct, so the sequence is read as a map by rules that use only the names or only
the types}.
