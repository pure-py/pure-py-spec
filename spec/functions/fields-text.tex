The fields of class $C$ with their declared types: the fields of its base class, if any, followed by the
fields declared by $C$. Field names are distinct, so the sequence is read as a map by rules that use only the names or only
the types. On a class value, the field names alone, in the same order, with the base class read from
the value.
