The fields of class $C$ with their declared types: the fields of its base class, if any, followed by the
fields $C$ declares. Field names are distinct, so the sequence is read as a map where only the names or only
the types are wanted.
