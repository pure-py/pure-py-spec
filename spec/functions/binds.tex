\binds(x) &= \set{x} \\*
\binds(p\;\pyinline{as}\;x) &= \binds(p) \cup \set{x} \\*
\binds(\varepsilon) &= \varnothing \\*
\binds(\qualGuard{e} \cons \seq{g}) &= \binds(\seq{g}) \\*
\binds(\qualGenerator{x}{e} \cons \seq{g}) &= \set{x} \cup \binds(\seq{g}) \\*
\binds(\exMatch{e}{\exCase{p_1}{s_1}\;\ldots\;\exCase{p_{n+1}}{s_{n+1}}}) &{}= \textstyle\bigcup_i \big(\binds(p_i) \cup \binds(s_i)\big) \\*
\binds(s\;s') &= \binds(s) \cup \binds(s')
