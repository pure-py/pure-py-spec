\added{Names declared anywhere in $t$, in textual order and with repeats, not descending into nested functions. It is empty on the statements not shown and is the concatenation over the branches of a conditional and the cases of a match, and on a module body $\seq{\iota}\;t$ acts on $t$.}
