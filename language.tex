\documentclass{scrartcl}

\usepackage{amsmath}
\usepackage{amssymb}
\usepackage{beramono}
\usepackage{enumitem}
\usepackage{etoolbox}
\usepackage{float}
\usepackage{libertine}
\usepackage{listings}
\usepackage{mathpartir}
\usepackage{microtype}
\usepackage[authoryear,round]{natbib}
\usepackage{titling}
\usepackage{subcaption}
\usepackage{xcolor}
\usepackage{xspace}

\input{tex/macros}
\input{tex/listings}

\raggedbottom
\setlength{\droptitle}{-8ex}
\setlist[itemize]{leftmargin=*, nosep}
\setlist[enumerate]{nosep}

\title{\textbf{\Lang: A Functional Subset of Python}}
\author{Nikolaus Huber\textsuperscript{1} \and Dominic Orchard\textsuperscript{2,1} \and Jacob Pake\textsuperscript{2} \and Roly Perera\textsuperscript{1,3}}
\date{}

\newcommand{\affiliations}{%
    \textsuperscript{1}\textit{Department of Computer Science and Technology, University of Cambridge, UK} \\
    \textsuperscript{2}\textit{School of Computing, University of Kent, UK} \\
    \textsuperscript{3}\textit{School of Computer Science, University of Bristol, UK}
}

% Override maketitle to avoid internal tabular conflicts
\renewcommand{\maketitle}{%
    \vspace*{\droptitle}
    \begin{flushleft}
        {\LARGE\bfseries \thetitle \par}
        \vspace{1ex}
        {\Large \theauthor \par}
        \vspace{1ex}
        {\affiliations \par}
    \end{flushleft}
    \vspace{2ex}
}

\begin{document}

\pagestyle{plain}

% Otherwise custom \maketitle allows floats to appear above title
\makeatletter
\let\orig@maketitle\maketitle
\renewcommand{\maketitle}{%
  \orig@maketitle
  \global\@topnum=0
}
\makeatother

\maketitle

\section{Introduction}

\begin{itemize}
\item Define syntactic subset of Python called \Lang
\item Define pure semantics for \Lang
\item Semantics of \Lang program should be identical to its semantics in Python
\end{itemize}

\section{Syntax}

\subsection{Environments}

Environments $\Gamma, \Delta$ are (not necessarily finite) maps from sets of variables to their \emph{definite
assignment} status, which is an element of the 2-point Boolean lattice $\set{\statusDefAssigned,
\statusNotDefAssigned}$. The top element $\statusDefAssigned$ indicates \emph{definitely assigned}, and the
bottom element $\statusNotDefAssigned$ indicates \emph{not definitely assigned}; we can think of these as
degenerate types (``any'' and ``none''). The definite assignment terminology is borrowed from Java.

\paragraph{Environment operations.}

Write $\Gamma \runion \Delta$ (``override'') for the right-baised map union of $\Gamma$ and $\Delta$. Also
define the following semigroup operation $\merge$ (``merge'') on environments:
%
\[\Gamma \merge \Delta := \set{x: \bot \mid \forall x \in (\Gamma \setminus \Delta) \cup (\Delta \setminus \Gamma)} \cup \set{x: \Gamma(x) \meet \Delta(x) \mid x \in \Gamma \cap \Delta}\]

\input{fig/syntax}

\subsection{Well-formedness}

\input{fig/assigns-returns}
\input{fig/wf}

\subsection{Examples}
\subsubsection{Unreachable code}
Every well-formed statement either \emph{returns}, or \emph{assigns} a set of variables $\Delta$, which may be
empty. Every function body must return; and a non-empty sequence of statements returns only if the last
statement returns, and all preceding statements assign. Thus the following is illegal:

\begin{python}
def foo(x):
  return x + 1
  return x    # error: unreachable
\end{python}

\subsubsection{Conditionals without \pyinline{else} clauses}

An \pyinline{if} statement without an \pyinline{else} clause is equivalent to an \pyinline{if} statement with
an \pyinline{else} clause whose body is a $\exPass$ statement; such a statement never returns.

\subsubsection{Implicit \pyinline{None}}

A \pyinline{return} statement without an expression is equivalent to \pyinline{return None}. A function whose
body $s$ assigns rather than returns is equivalent to a function whose body is $s$ followed by
\pyinline{return None}.

\subsubsection{Local assignment in conditional clauses}

For flexibility, we permit ``branch-local'' variables. The assignment of \pyinline{x} below (left) is
permitted, and can be used locally in that branch of the conditional:

\begin{figure}[H]
\centering
\begin{subfigure}{0.48\textwidth}
\centering
\begin{python}
def foo():
  z = 6
  if b:
    x = 3
    y = x + 1
  else:
    y = 0
  return z
\end{python}
\caption{Branch-local definition of \pyinline{x}}
\end{subfigure}
\hfill
\begin{subfigure}{0.48\textwidth}
\centering
\begin{python}
def foo():
  x = 6
  if b:
    x = 3
    y = x + 1
  else:
    y = 0
  return x # error: partially defined
\end{python}
\caption{Now with use of \pyinline{x} after the conditional}
\end{subfigure}
\end{figure}

\noindent However such variables only appear to be branch-local. The definition of $\merge$ actually ensures
that a variable assigned in any branch is considered assigned by the conditional; however unless it is
assigned in \emph{every} branch, it will be bound to $\bot$ in the corresponding environment. Any attempt to
refer to such a variable is illegal, as it represents a variable which is only partially assigned. (Thus the
right-hand program is ill-formed, despite the enclosing \pyinline{x = 6}, because in the \pyinline{return x}
statement that outer assignment is shadowed by the inner one, and that inner assignment is only partial.) If
we were to allow the program on the right, there would be no straightforward way to assign it a semantics
which is both pure, and in agreement with Python, since in Python the function returns \pyinline{3} in the
case where \pyinline{b} is \pyinline{True}.

\end{document}
